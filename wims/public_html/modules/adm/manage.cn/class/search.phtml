<p>
!if $overflow=yes
 !if $keyword = $empty
  本服务站有许多虚拟班. 为寻找一个班级, 请输入一个关键词.
 !else
  还是有太多班级与你的关键词对应. 请给出更精确的词.
 !endif
!endif
!if $keyword!=$empty or $overflow=yes
 <p><center>
 !form reply
  <input size=20 name=keyword value="$keyword">
  <input type=submit value=OK>
  </form></center> <p>
!endif

