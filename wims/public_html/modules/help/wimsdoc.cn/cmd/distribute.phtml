!! +exec+ +vardef+
tit:distribute
type:$type_exec
syntax:!distribute `type string' into `namelist'
mean:把 `string' 的内容分配给变量 `namelist'.\
  根据 `type' 的取值(可以是 words, items, lines, chars),\
  分配按词、按项目、按行或按字符进行. <p>\
  例如, 如果 `type' 是 <tt>words</tt>,\
  则 `namelist' 里的第一、第二...个变量将依次被设置为\
  `string' 里的第一、第二...个词.
