# record 1: header: parent, keywords
# record 2: calling parameters
# record 3: submit buttons
# record 4: html form content

:analyse, fonction, limite, limite partielle, infinie,
limite partielle d'une fonction

:datatype, fonction
operation, limite

:, Limite partielle

:<TD VALIGN=TOP ALIGN=LEFT>
 $submit1
 <TD WIDTH=100% NOWRAP>f(x) = 
 <INPUT NAME="formula" TYPE="text" SIZE=25 VALUE="x/abs(x)"> <BR>
 pour
 <INPUT NAME="variable" TYPE="text" SIZE=3 VALUE="x"> vers
 <INPUT NAME="formula2" TYPE="text" SIZE=10 VALUE="0">
 <SELECT NAME="option">
 <OPTION VALUE="plus">par la droite
 <OPTION VALUE="minus">par la gauche
 <OPTION VALUE="both">des deux côtés
 </SELECT>
 
