slib_lang_exists_tmp=yes

slib_title=Fonction de répartition de la loi binomiale

slib_out=Valeurs en <span class="tt">data</span> de la fonction de répartition de la loi binomiale de paramètres\
 <span class="tt">n</span> et <span class="tt">p</span> (<span class="tt">n</span> est un entier et 0&lt;p&lt;1).
slib_comment=<span class="tt">data</span> peut être une liste d'entiers entre crochets.\ 
P(X=k)=binomial(n,k)*p^k*(1-p)^(n-k) pour k=0,...,n
