!! +var+ +string+
$lst_tit rename$lst_titend$type_both
  $(lst_syntax)rename $emph f$emphend</tt>
  $lst_mean 把原来可用 http 直接得到的文件 $emph f$emphend 改用别的称呼,
  使用 <tt>!getfile</tt> 的方式调用.
  <p>
  本命令对于那些通过多媒体文件(图像, 声音, 电影)传递重要信息的练习尤其有用.
  它可以用来隐藏多媒体文件的名字, 否则会对问题的解决提供重要线索.
  <p>重命名的文件必须放在模块里, 而文件名 $emph f$emphend 必须以 <tt>$module_dir</tt> 起首.
  <p>此命令返回一个串, 就是新的 URL. 文件在服务器里没有真的被复制或改名.
