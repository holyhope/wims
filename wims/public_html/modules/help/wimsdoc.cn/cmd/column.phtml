!! +var+ +exec+ +string+
$lst_tit column$lst_titend$type_both
  $lst_alias columns<br>
  $(lst_syntax)column $emph numlist$emphend of $emph string$emphend</tt> <br>
  $lst_mean 输出按照 $emph numlist$emphend 在矩阵 $emph string$emphend 里选出的列(用逗号分隔).
  矩阵的行可以用换行号或分号 ``;'' 分隔. <p>
  !set numlist_type=列
  !read cmd/numlist
  <p>
  如果只选一列, 则输出用逗号分隔, 否则按照矩阵 $emph string$emphend 的分隔方式.


