
!if $levelcnt<=0
 !exit
!endif

Hier is de backupfile:
!getfile gateway.csv gateway.csv
<p>
Dit is een spreadsheet file in CSV formaat (kommata  gescheiden datafile).<br>
De betekenis van de velden:
<p>
type, code,
titel, supervisor_account, klasse_aanmeldings_wachtwoord, docent_wachtwoord, opties.

