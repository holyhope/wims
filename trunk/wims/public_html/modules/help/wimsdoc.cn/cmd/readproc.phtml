!! +exec+ +jump+
tit:readproc
type:$type_exec
syntax:!readproc `file' [`parm']
mean:读入变量处理文件 `file'.\
  `file' 必须是以模块主目录作为基准目录的有效文件名.\
  (可在 `file' 注明目录结构, 但禁止后退到更高层的目录.)\
  <br/>在被调用的文件 `file' 里可以用变量名\
  `wims_read_parm' 得到参数 `parm'.
