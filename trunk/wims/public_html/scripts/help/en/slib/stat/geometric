slib_lang_exists_tmp=yes

slib_title=Generation of random data with a Geometric distribution on N

slib_out=Random data of <span class="tt">m</span> reals following a Geometric distribution with parameter <span class="tt">p</span> on N.\
If <span class="tt">m</span> is a list <span class="tt">[r,s]</span>, it returns an <span class="tt">r</span> by <span class="tt">s</span> matrix. 
slib_comment=P(X=k)=p(1-p)^k for every nonnegative integer k
