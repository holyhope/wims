slib_lang_exists_tmp=yes

slib_title=graphe orienté sans circuit
slib_parms=5\
,n\
,s nombre maximum de successeurs\
,texte pour les noms des sommets\
,couleur des arcs\
,couleur des sommets
raw, option : <tt>raw</tt>, <tt>url</tt>, <tt>html</tt>

slib_out=Génération d'un graphe sans circuit à utiliser avec flydraw quand l'option est raw\
ou est vide : draw source, \
matrix, matrice des rangs, matrices des positions du sommet, \
position des points
slib_comment=<tt>s</tt> peut être remplacé par une matrice correspondant à un tel graphe (aucune vérification n'est effectuée). \
Dans ce cas, seuls 4 paramètres sont requis.\
<br>Le premier élément du résultat est : par défaut le code flydraw ; \
l'adresse de l'image générée si l'option est <tt>url</tt> ; \
le code HTML pour insérer l'image est déjà inséré pour l'option <tt>html</tt>.\
Si l'option est <tt>url</tt> ou <tt>html</tt>, les deux derniers éléments représentent la taille de l'image.
