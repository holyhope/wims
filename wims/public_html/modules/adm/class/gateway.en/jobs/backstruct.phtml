
!if $levelcnt<=0
 !exit
!endif

Here is the backup file:
!getfile gateway.csv gateway.csv
<p>

This is a comma-separated data file. Meaning of the fields: 
<p>
type, code,
title, supervisor-account, class-password, supervisor-password, options.

