
!if $wims_user!=$empty
 !read direct.phtml
 !if $wims_user!=supervisor and $supervisable!=yes
  !exit
 !endif
 <p>
!else
 Las clases virtuales le permiten organizar el trabajo de sus 
 alumnos/estudiantes utilizando los recursos de este sitio WIMS y
 seguir sus progresos. Puede también añadir ejercicios y documentos que 
 cree usted.
 <p>
 Si aún no ha creado su propia clase, puede
 !href module=adm/class/regclass crear una
  tras haber consultado las
 !href target=wims_help module=help/main&chapter=3 páginas de ayuda
, o echar un vistazo a las
 !href module=$module&type=example clases de ejemplo
. <p>
!endif

!if $clcnt+$sccnt>0
 !if $wims_user=$empty
  De lo contrario, pulse sobre el nombre de su centro educativo o clase
  para entrar como profesor.
  La inscripción a esta parte está reservada a los profesores.
 !endif
 !if $sccnt>0
  <p>
  $table_header
  <caption>Centros y grupos de clases</caption>
  $table_hdtr<th>Nombre</th><th>Administrador</th>
  <th>-</th></tr>
  !for i=1 to $sccnt
   !set l=!line $i of $sclasses
   !distribute items $l into code,expire,in,cl,la,ty,lv,pswd
   !set supv=!item 9 of $l
   !default supv=!defof class_supervisor in wimshome/log/classes/$code/.def
   
   $table_tr
   <td align=center>
   !href module=$module&type=authparticipant&class=$code $in
   </td>
   </td><td align=center>$supv</td>
   <td align=center>
   !href module=adm/class/reguser&class=$code Inscribirse
&nbsp;
   !href module=$module&type=authsupervisor&class=$code Administrar
   </td>
  !next i  
  $table_end
 !endif
 !if $clcnt>0
  <p>
  $table_header
  !if $superclass=$empty
   <caption>Clases independientes</caption>
   $table_hdtr<th>Centro educativo<th>Clase<th>Profesor
  !else
   !if $splevel>1
    !set inst=!defof class_description in wimshome/log/classes/$superclass2/.def
   !else
    !set inst=!defof class_institution in wimshome/log/classes/$superclass/.def
   !endif
   <caption>$Name_levels en $inst
   !if $splevel>1
    !href cmd=reply&type=$type&superclass2=$sparent Arriba
   !endif
   <caption>
   $table_hdtr<th>$Name_level<th>Profesor
   !if $superclass!=$empty and $stype=4
    <th>+
   !endif
  !endif
  !for i=1 to $clcnt
   !set l=!line $i of $classes
   !distribute items $l into code,expire,in,cl,la,ty,lv,pswd
   !set supv=!item 9 of $l
   !default supv=!defof class_supervisor in wimshome/log/classes/$code/.def
   
   $table_tr
   !if $superclass=$empty
    <td align=center>$in</td>
   !endif
   <td align=center>
   !href module=$module&type=authsupervisor&class=$code $cl
   </td><td align=center>$supv</td>
   !if $superclass!=$empty and $stype=4
    <td>
    !if $ty!=1
     !href cmd=reply&type=$type&superclass2=$code $sousclasses
    !else
    -
    !endif
   !endif
  !next i
 !endif
 $table_end
!else
 !if $class_type=$empty and $superclass=$empty
  (este sitio no contiene ninguna clase en español).
 !else
  (no hay ninguna clase disponible).
 !endif
!endif
<p>

