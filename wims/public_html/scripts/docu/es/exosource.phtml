
<center><h1>$tit</h1></center>

!if $error!=$empty
 !read docu/es/msg.phtml
 <p><hr> <p>
!endif

!if $titb=$empty
 <p>
 El bloque <tt>$block</tt> no ha sido creado todavía.
 <p>
 !exit
!endif

!if $datetest<0
 <p>
 Esta página todavía no está lista para su lectura. ¡Regrese más tarde!
 <p>
 !exit
!endif

!if $datetest>0
 <p>
 Esta página ha expirado. Ya no puede leerla, lo sentimos.
 <p>
 !exit
!endif

<h3>$titb</h3>

!if $sheetdata=$empty
 <p>
 No hay datos de ejercicios en esta página.
 <p>
 !exit
!endif

Aquí tiene los ejercicios de esta página en el formato de hoja de 
trabajo. Puede usar `recortar y pegar' para insertarlos
!if $wims_user=supervisor
 en la hoja que elija en su clase.
!else
 en la hoja de una clase virtual de la que usted sea responsable.
!endif

<pre>
$sheetdata
</pre>

</DIV><DIV CLASS=wimstail>
<p><hr><p>

<center>
!href cmd=reply&job=read Volver a doc
</center> <p>

