slib_lang_exists_tmp=yes

slib_title=graphe orienté sans circuit
slib_parms=5\
,n\
,s nombre maximum de successeurs\
,texte pour les noms des sommets\
,couleur des arcs\
,couleur des sommets
raw, option : <span class="tt">raw</span>, <span class="tt">url</span>, <span class="tt">html</span>

slib_out=Génération d'un graphe sans circuit à utiliser avec flydraw quand l'option est raw\
ou est vide : draw source, \
matrix, matrice des rangs, matrices des positions du sommet, \
position des points
slib_comment=<span class="tt">s</span> peut être remplacé par une matrice correspondant à un tel graphe (aucune vérification n'est effectuée). \
Dans ce cas, seuls 4 paramètres sont requis.\
<br/>Le premier élément du résultat est : par défaut le code flydraw ; \
l'adresse de l'image générée si l'option est <span class="tt">url</span> ; \
le code HTML pour insérer l'image est déjà inséré pour l'option <span class="tt">html</span>.\
Si l'option est <span class="tt">url</span> ou <span class="tt">html</span>, les deux derniers éléments représentent la taille de l'image.
