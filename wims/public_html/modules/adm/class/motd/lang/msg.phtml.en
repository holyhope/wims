!set wims_module_log=error: $error

!if not_supervisor=$error
 Sorry, but only the registered supervisor of a class has the right to
 write a message of the class.
 !exit
!endif

!if bad_class=$error
 Strange, but I find that your class is not valid!
 !exit
!endif

!if bad_user=$error
  Strange, but I do not find this participant in this class !
 !exit
!endif