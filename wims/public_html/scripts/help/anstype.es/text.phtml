
<b>Texto (sensible o no al tipo de carta: mayúsculas/minúsculas).</b>
(nombres : <tt>case</tt> et <tt>nocase</tt>)
<br>
La respuesta se compara como texto a una buena respuesta dada.
En el caso <tt> archiva< a /tt> (sensible a mayúsculas/minúsculas),
cada palabra de la respuesta debe ser exactamente la misma que la palabra
correspondiente de la buena respuesta. Las expresiones del tipo e'
se transforman en é. Para evitarlo, utilizar la opción <tt> noreaccent< a /tt>.

!read help/anstype.fr/syno.phtml

