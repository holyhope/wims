!! +exec+ +string+
tit:eval
type:$type_var
alias:evalue
syntax:!eval `expr'
mean:返回表达式 `expr' 的数值,\
  而且是实数(对于目前的wims版本). 内部计算是以双精度进行的,\
  但打印精度由变量 `wims_print_precision' 确定(其默认值是 8). <br/>\
  如果在表达式 `expr' 内出错, 则返回 0.\
  (我知道这不好; 但目前我还没有时间搞定.)
