
!if $clcnt<=0
 Sorry, no example class has been found on this site.
 !exit
!endif

!if $lacnt<=0
 There is no class of English language on this site.
 <p>
!endif

There are $clcnt example classes in English on this site. 
They intend to  to show you how virtual classes work from a students' side.
If you want to experiment  with a virtual class as a supervisor, you will first have to
!href module=adm/class/regclass create
 one. Click on the link and follow the instructions.

<p>$table_header
$table_hdtr<th>Class name<th>Level<th>connection type<th>Register
!for i=1 to $clcnt
 !set l=!line $i of $classes
 !distribute items $l into code,expire,inst,cl
 !defread wimshome/log/classes/$code/.def
 !distribute char $class_level into l_,y_
 
 $table_tr<td align=center>$inst<br>$cl</td>
 <td align=center>$(n_$l_)<br>year $y_</td>
 <td align=center>
 !href module=home&user=anonymous,$code,anonymous Anonymous
 &nbsp;
 !href module=$module&type=authparticipant&class=$code Registered
 </td><td align=center>
 !href module=adm/class/reguser&class=$code Register
 </td>
!next i
</tr>$table_end <p>
 
<ul>
<li>To enter the class as an anonymous visitor (participant), click on <tt>Anonymous</tt>.
<li><p>If you wish to use your own user name and password in the example classes,
you will first have to register. Click on <tt>Registered</tt> to do this.

<li><p>Once you have registered, you can enter the class by clicking
on <tt>Registered</tt>. Please note : Registration data are regularly erased 
from the server in example classes.
</ul>

