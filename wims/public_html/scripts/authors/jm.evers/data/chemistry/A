#!/bin/sh

exit
file=n_alkynes.def
echo "# schem:
# :record name
# dutch_name english_name french_name... [words]
# WIMSchem data


">$file

for i in `ls *yne` ; do

    echo ":$i" >>$file
    echo "$i $i $i" >>$file
    cat $i >> $file
    echo " " >> $file
    echo " " >> $file
done