!set wims_module_log=error: $error
!set level=$[$level-1]

<center><h3>Error</h3></center> <p>

!if no_title iswordof $error
 Por favor, dé un título a este ejercicio en preparación.
 !exit
!endif

!if empty_title iswordof $error
 El software es incapaz de encontrar el título de su ejercicio. ¿Lo ha definido?
 !exit
!endif

!if no_answer iswordof $error
 Su ejercicio debe pedir al menos una respuesta (numérica, funcional 
 u opciones múltiples).
 !exit
!endif

!if no_statement iswordof $error
 El contenido de su ejercicio está incompleto: no ha definido el enunciado
 del ejercicio.
 !exit
!endif

!if no_reply2 iswordof $error
 El contenido de su ejercicio está incompleto: no hay peticiones válidas de 
 respuestas (una petición de respuesta es válida solamente si se han 
 completado todos los campos).
 !exit
!endif

!if def_fail iswordof $error
 El software no podido generar un ejercicio válido a partir de los
 datos que ha introducido. Hay errores en lo que ha escrito.
 <p><pre>$oef2wims_out</pre> <p>
 !if $mode!=$empty
  !set level=1
 !else
  !set level=2
 !endif
 !exit
!endif

!if name_exhaust iswordof $error
 Hay ya varios ejercicios en su clase que tienen el mismo título que el
 que acaba de crear. Si estos ejercicios son REALMENTE diferentes, 
 elija otro título para el ejercicio ejercicio actual antes de grabarlo.
 !exit
!endif

!if name_exhaustpub iswordof $error
 Se han enviado ya varios ejercicios con el mismo título que el que
 quiere crear. Si no está enviando el mismo ejercicio varias veces,
 por favor, elija otro título para su ejercicio actual antes de grabarlo.
 !exit
!endif

!if unknown iswordof $error
 El registro de su ejercicio ha fallado por alguna razón desconocida. Lo sentimos.
 !exit
!endif

!if already iswordof $error
 El ejercicio que quiere enviar ya está en el servidor.
 !exit
!endif

!if not_class iswordof $error
 Su solicitud es ilegal.
 !exit
!endif

!if not_supervisor iswordof $error
 Hmm... ¡Por supuesto, solamente los profesores pueden borrar los ejercicios
 de la clase!
 <p>Su solicitud se ha rechazado.
 !exit
!endif

!if not_exist iswordof $error
 !if $catmodule!=$empty
  El ejercicio <tt>$del</tt> de <tt>$catmodule</tt>
  que desea importar no existe o usted no está autorizado a
  importarlo.
 !else
  El ejercicio <tt>$del</tt> que quería modificar/eliminar no existe en su clase.
 !endif
 !exit
!endif

