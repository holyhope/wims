!! +exec+ +var+ +vardef+
tit:module
type:$type_both
syntax:module `defname' `modname'
mean:返回模块 `modname' 对 `defname' 的定义.\
  最常用的是返回模块的名称(`defname'=title). <br/>\
  将在模块 `modname' 的 INDEX 文件里搜寻 `defname' 的定义.
