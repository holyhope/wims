!! +exec+ +jump+
tit:read
type:$type_exec
alias:include, input
syntax:!read `file' [`parm']
mean:插入 `file' 的内容并加以处理.\
  `file' 必须是以模块主目录作为基准目录的有效文件名.\
  (可在 `file' 注明目录结构, 但禁止后退到更高层的目录.)\
  <br/>在被调用的文件 `file' 里可以用变量名\
  `wims_read_parm' 得到参数 `parm'. <br/>\
  WIMS将考虑对文件作缓存以供重复使用, 除非在文件名前冠以 "./".
