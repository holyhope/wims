
<h2 class="wims_center">>根据回答确定反馈信息</h2>

你可以根据用户的回答制定一些条件, 若这些条件满足, 就可在答案页上打印一些信息.
通常用于出现典型错误时提出警告.
<p>
可以对各种形式的回答(自由格式或多重选择)加入任意多个反馈信息.
其语法如下:
</p><p><span class="tt">
\feedback{条件}{信息}
</span> </p><p>
这行命令可以放在参数区域内 (指导模式) 或源码的任何地方 (源码模式).
</p><p>
当 <span class="tt">条件</span> 满足时,
<span class="tt">信息</span> 将会显示在答案页上. 用户的回答可以使用变量 <span class="tt">\reply1</span>,
<span class="tt">\reply2</span>,... (对自由格式), 或 <span class="tt">\choice1</span>,
<span class="tt">\choice2</span>,... (对多重选择). 变量的次序就是它们出现在输入中的次序
(或者是指导模式的表格中的次序).
</p><p>
<span class="tt">信息</span> 是自由格式的文本, 也能使用嵌入的 html tag 以及数学符号.

</p><p>
也可以使用变量 <span class="tt">\sc_reply1</span>, <span class="tt">\sc_reply2</span>,
... 当用户回答正确时取值 1, 回答错误时取值 0,
在数值精度范围内的正确回答取值 0.5. 这个变量不考虑条件,
而且当回答需要根据条件来分析时取空值.

</p>$table_header
<caption>条件的例
<small>
!href target=wims_mhelp cmd=help&special_parm=if 全部条件
</small>
</caption>
$table_hdtr<th>条件</th><th>何时被满足</th></tr>
$table_tr<td class="tt wims_code_words">\reply1=5</td><td>第一个自由格式回答正好等于 5</td></tr>
$table_tr<td class="tt wims_code_words">\reply1&gt;\reply2+2</td><td>第一个自由格式回答 &gt; 第 2 个加 2</td></tr>
$table_tr<td class="tt wims_code_words">\choice1=Oui and \choice2=Non
</td><td>第一个多重选择是 Yes, 第 2 个是 No (<b>注意</b> 多重选择里的比较是区分大小写的!)
$table_tr<td class="tt wims_code_words">\reply1&lt;5 or \reply1&gt;8
</td><td>第一个回答在区间 (5,8) 以外.</td></tr>
$table_tr<td><tt>3 isitemof \reply1
  </tt></td><td>类型为 "set" 或 "vector" 的第一个回答中包含元素 "3".</td></tr>

$table_end
<p>
<b>注意</b>. 在同一个条件里不要同时使用 <span class="tt">\reply</span> 与 <span class="tt">\choice</span>.
这会导致不可预测的结果.
</p>
