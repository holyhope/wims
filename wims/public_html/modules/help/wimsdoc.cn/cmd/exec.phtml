!! +exec+ +var+
tit:exec
type:$type_both
alias:execute, run, call
syntax:!exec `command parms'
mean:执行具有可选参数 `parms' 的外部命令 `command'.\
  `command' 必须是在wims公共目录 `bin' 里面的有效程序名. <br/>\
  当作为变量命令时, 返回调用程序的输出.
