
!read adm/title.phtml 3\
\
$title
<center>
!href cmd=reply&job2=def $nei_description
 de
<em><font color=blue>$nei_institution</font></em>
($wims_name_nameteacher:
<a href="mailto:$nei_email?subject=Class neighboring
relations">$nei_supervisor</a>)
</center>
<p>
La classe <em><font color=blue>$nei_description</font></em>
vous permet de partager ses ressources suivantes.
<small>
!href cmd=help&special_parm=share $wims_name_help
</small>
<ul>
!for r in $nsharable
 <li><p><em>$(name_$r)</em> ($(it_$(r)cnt) disponible).
 !if $r isitemof $sharing or $(sharable_$r)!=$empty
  !if $r isitemof $sharing
   !if $(sharing_$r)=$nei
    Vous êtes en cours de partage.
    !href cmd=reply&job2=stopshare&stoprec=$r $name_stopsharing
. <small>
    !href cmd=help&special_parm=stopshare $wims_name_help
    </small>
   !else
    Vous ne pouvez pas les partager car vous partagez déjà celles
    d'une autre classe.
   !endif
  !else
   Vous ne pouvez pas les partager car vous permettez à d'autres classes
   de partager VOS ressources du même type. Les partages en cascade ne sont pas
   autorisés. Donc vous devez d'abord arrêter les permissions de partage
   avec l'autre classe
   avant de pouvoir partager celles de la classe 
   <em><font color=blue>$nei_description</font></em>.
  !endif
 !else
  !if $(me_$(r)cnt)>0 and $r isitemof $nosharelist
   Vous ne pouvez pas les partager car vos propres $(name_$r) ne sont pas
   vides. Avant le partage, vous devez d'abord
   !href cmd=reply&job=clean nettoyer
   vos propres ressources (si c'est possible).
  !else
   !href cmd=reply&addshare=$r $name_share
.
   !if $(me_$(r)cnt)>0
    <b><font color=red>$wims_name_warning</font></b>.
    !if $r=sup
     &Egrave; molto pericoloso condividere l'account docente!
     !href cmd=help&special_parm=supshare $wims_name_detail
    !else
     Le partage va EFFACER vos $(name_$r) existant(es)&nbsp;! 
     Elles seront définitivement perdues si vous n'avez pas de sauvegarde.
    !endif
   !endif
  !endif
 !endif
!next r
</ul>

