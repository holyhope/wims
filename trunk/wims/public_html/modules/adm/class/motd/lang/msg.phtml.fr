!set wims_module_log=error: $error

!if not_supervisor=$error
 Désolé, mais seul l'enseignant enregistré d'une classe a le droit d'écrire un message dans la classe.
 !exit
!endif

!if user_notconnected=$error
 Ce participant n'est plus connecté !
 !exit
!endif

!msg $error
