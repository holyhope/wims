slib_lang_exists_tmp=yes
slib_title=Compteur avec aiguille configurable.

slib_parms=5\
,position de l'aiguille: nombre dans l'intervalle du paramètre suivant (entre -7 et 7)\
[-6,6],intervalle dans [-7 , 7]\
[0,,20,,40,,60,,80,,100,,120],textes à mettre sur les graduations\
[black,black,black,transparent],couleurs: [graduation,texte,aiguille,fond]\
raw,option mot : <span class="tt">url</span>, <span class="tt">html</span> ou <span class="tt">raw</span>

slib_out= Compteur avec aiguille configuré.

slib_comment=Le premier item de la sortie est : par défaut, le code flydraw; \
l'adresse de l'image produite si il y a le mot d'option <span class="tt">url</span> ; \
le code html pour insérer l'image est dans ce cas déjà inséré avec l'option<span class="tt">html</span>.