!set titb=The command <tt>\embed</tt>
!set keyw=
!set datm=20110713
!set prev=
!set next=
!set upbl=syntax
!set dat1=19000101
!set dat2=24000101

!if $wims_read_parm!=$empty
 !goto $wims_read_parm
!endif

!exit

:content
<br>
 Insert another block&#58; up to 2 arguments.<ul>
  <li>Argument 1&#58; name of the block to insert.
  </li><li>Argument 2 (optional)&#58; a text to replace the title of the cited block.
 </li></ul>
For example, <tt class="wims_code_words">\embed{embedded}</tt> insert the bloc <tt class="wims_code_words">embedded</tt> 
into the current block.
<p>
<p>
One can also insert the block of a public document. In this case, the 
first argument 
is the address of the document and 
the third argument is the name of the block to insert  (for the moment,
 the second one is not useful in this case) &#58;
for instance, <tt class="wims_code_words">\embed{H2/algebra/docrelatif}{}{block=abscisse}</tt>
<p>

!read primitives.phtml 1, embed, H2/algebra/docrelatif, , block=abscisse 

