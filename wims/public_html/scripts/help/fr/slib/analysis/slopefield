slib_lang_exists_tmp=yes

slib_title=Champ de direction (par exemple pour un système différentiel)

slib_out= Dessine le champ de direction d'un système différentiel x' = f1 , y' = f2 \
  where f1 and f2 are expressions in x and y.
  
slib_comment=Dans le cas de l'option js, on peut donner le numéro de l'applet\
 (écrire js=1 par exemple).
