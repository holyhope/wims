!! +exec+ +vardef+
$lst_tit let$lst_titend$type_exec
  $lst_alias def, define, set<br>
  $(lst_syntax)let $emph name$emphend=$(emph)value$emphend</tt> <br>
  $lst_mean put $(emph)value$emphend into the variable $(emph)name$emphend. <br>
  $(emph)value$emphend may be a variable command (together with optional parameters).
