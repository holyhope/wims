!!<html><head>
!!$wims_html_header
!!</head>
!!<body $wims_htmlbody>
!!!headmenu
!header1

!if $cmd=help and $special_parm=about
 !read about.phtml
 !goto end
!endif

!if $cmd=help
 !read help.phtml
 !goto end
!endif

<h1 class="wims_title">WIMS op $httpd_HTTP_HOST
<br>Statistische gegevens</h1>
<p><hr> <p>

!if $sitestart=$empty
    De bezoekstatistieken zijn op deze server gedeactiveerd. 
  <p>
 !goto end
!endif

!if $show=yes and $total_site>0 and $laps>0
!!if $show=yes
 !href cmd=resume Terug naar het menu
 <p>
 !read answer.phtml
 !set wims_menu_items=!append line back2,1,cmd=resume\
 to $wims_menu_items
 !!!href cmd=resume Retour au menu
 <p>
!else
 !if $usercnt>0
  Er zijn $classcnt instituten en individuele klassen op deze server geinstalleerd met daarin
  $usercnt gebruikers.
  <p>
 !endif
 !read menu.phtml
!endif
:end
!homeref
!tail

!set stat_module=
