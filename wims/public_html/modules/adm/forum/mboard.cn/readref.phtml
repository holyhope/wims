 <small>
 !if $dcnt>1
  [
  !if $dlast!=$empty
   !href cmd=reply&job=read&r_msg=$dlast &lt;&lt;
   &nbsp;
  !endif
  !href cmd=reply&job=list 按日期
  !if $dnext!=$empty
   &nbsp;
   !href cmd=reply&job=read&r_msg=$dnext &gt;&gt;
  !endif
  ]
 !endif
 !if $r_prec!=$empty
  &nbsp;
  !set t_=!translate / to , in $r_prec
  !href cmd=reply&job=read&r_msg=$r_prec 前一个
 !endif
 !if $repcnt>0
  &nbsp;
  !href cmd=reply&job=read&r_msg=$r_rep1 第一个跟帖
  &nbsp;
  !href cmd=reply&job=thread&th_orig=$l_thread&th_start=$r_msg 所有跟帖
 !endif
 !if $thtcnt>1
  &nbsp;&nbsp;[
  !if $tlast!=$empty
   !href cmd=reply&job=read&r_msg=$tlast &lt;&lt;
   &nbsp;
  !endif
  !href cmd=reply&job=thread&th_orig=$l_thread&th_start=$l_thread 线索
  !if $tnext!=$empty
   &nbsp;
   !href cmd=reply&job=read&r_msg=$tnext &gt;&gt;
  !endif
  ]
 !endif
 </small>

