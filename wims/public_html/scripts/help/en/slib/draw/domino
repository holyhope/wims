slib_lang_exists_tmp=yes

slib_title=Domino
slib_parms=6\
,number of points in the first square\
,number of points in the second square\
black,color\
H,direction (V or H)\
,text if one of the numbers of points is 0\
raw,option word : <tt>url</tt>, <tt>html</tt> or <tt>raw</tt>

slib_out=flydraw code for a domino, url or code html according to the option, size of the image\
if the option word is <tt>url</tt> or <tt>html</tt>
slib_comment=the output is: by default, the flydraw code; \
the address of the generated image if the option word is <tt>url</tt> ; \
the html code for inserting image is already inserted for the option <tt>html</tt>.