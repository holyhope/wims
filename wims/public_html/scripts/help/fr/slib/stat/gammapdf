slib_lang_exists_tmp=yes

slib_title=Densité de probabilité de la loi Gamma

slib_out=Valeurs en <span class="tt">data</span> de la densité de probabilité\
 de la loi Gamma de paramètres <span class="tt">a</span> et <span class="tt">b</span>>0 (2 réels strictement positifs).
slib_comment=<span class="tt">data</span> peut être une liste de réels entre crochets.\
La densité en x est b*exp(-b*x)*(b*x)^(a-1)/gamma(a) pour x>=0.

