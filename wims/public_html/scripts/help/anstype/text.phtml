

 <b>Text (case sensitive or non-sensitive).</b>
 (names: <tt>case</tt> and <tt>nocase</tt>)
 <br>
 This is a text input to be compared with a given good answer. In the
 case-sensitive type, each word of the input must be exactly the same as 
 the corresponding word of the good answer.
 
 Expressions as  e' are transformed in é (reaccentuation). 
If you don't want that, use  option <tt>noreaccent</tt>. 


 !read help/anstype/syno.phtml

