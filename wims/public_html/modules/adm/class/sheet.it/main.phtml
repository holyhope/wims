!reset confirm
!!<html><head>
!!$wims_html_header
!!</head><body $wims_htmlbody onload=$wims_html_onload>
!!headmenu
!read adm/css.phtml
!header1

!read names.phtml

!if $job iswordof student
 !changeto $job.phtml
!endif

!if $cmd=help
 !read help/$special_parm.phtml
 !goto end
!endif

!read adm/title.phtml 1\
$wims_classname - $wims_institutionname\
$name_title

<p>
!if $error!=$empty
 <p>
 !read msg.phtml
 <p>
 !goto end
!endif

!if $job=getsource
 !read getsource.phtml
 !goto end
!endif

!if $activetest>-1
 <b>Informazioni generali:</b>
!endif
!read adm/sheet/sheetinfo.phtml

!ifval $activetest=-1
 !goto end
!endif

:end1
<a name=list></a>
<b>Contenuto del test di autovalutazione:</b>
!if $exocnt=0
 <p>Il test non contiene ancora alcun esercizio.
!else
 <small>[
 !href module=adm/sheet&sh=$sheet&job=print $wims_name_printable
 ]</small>
 !read adm/sheet/sheetcontent.phtml
!endif

!if $activetest=0
 <p>Per inserire un elemento (esercizio, cours, strumento, etc.) in un test 
 di autovalutazione è necessario selezionare l'elemento (vedi sotto),
 configurarne le opzioni e avviarlo. In questo modo alla base della pagina 
 che si apre compare un link che permette l'inserimento nel test.

 Per scegliere gli elementi potete
 <ul>
  <li> utilizzare il motore di ricerca:
	!read adm/search_engine/form.phtml
 </li>
 <li> oppure utilizzare lo strumento di 
 !href module=help/teacher/program confronto 
 tra i programmi di insegnamento francese e gli esercizi di WIMS.
</li>
   <li> 
    !href cmd=reply&job=prep_putsource $wims_name_putsource
   </li>
</ul>
!endif

:end
!if $activetest>=0
 !if $activetest=0
  !let wims_menu_items=!append line putsource,1,cmd=reply&job=prep_putsource to $wims_menu_items
 !endif
 !if $job!=getsource
  !let wims_menu_items=!append line sheetsource,1,cmd=reply&job=getsource to $wims_menu_items
!endif
 !let wims_menu_items=!append line ,0,\
printable,1,module=adm/sheet&sh=$sheet&job=print\
participantview,1,module=adm/sheet&sh=$sheet to $wims_menu_items
!endif

!tail

!distribute item ,, into job,source,title,desc,prpage

