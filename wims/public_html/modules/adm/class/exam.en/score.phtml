
!if $job=score
 <center><h1>$wims_classname, $wims_institutionname<br>$title</h1>
 !if $wims_user=supervisor
  (Presentation for participants)
 !endif
 </center> <p>$desc<p>
!endif

!if $error!=$empty
 <p>
 !read msg.phtml
 <p>
 !href module=home Back to home page
. <p>
 !goto end
!endif

!if $exocnt=0
 The exam is empty! Nothing is assigned in it. Possibly an 
 error in the manipulation by your supervisor.
!else
 <p><center><table border=1 bgcolor=bisque>
 <caption>Current score detail
 <tr><th>Exercise<th>Title<th>Weight<th>Score
 !for i=1 to $exocnt
  !set ex=!record $i of wimshome/log/classes/$wims_class/exams/.exam$exam
  !distribute lines $ex into we,li,ti
  <tr><td align=center>$i
  <td align=center>$ti
  <td align=center>$we<td align=center>
  !if $(replied$i)>0
   $(got$i)
  !else
   <small>pending</small>
  !endif
 !next i
 <tr><td align=center colspan=4>
 !default score=$sprime
 Score of this exam session: <b>$score</b> <small>over 10</small>.
 $table_end
!endif
<p>

!if $registered=$empty
 You have not yet started your work on the exam.
 !href cmd=reply&job=student Go to start the work.
!else
 !if $timeleft<=0
   Time is up!
   You have used up the $sdure minutes allocated for this exam.
   <p>
 !endif
 !if $allowtype=simulation
  The current score is not taken into account, because
  the exam is in simulation mode.
  !if $lastscore>0
   (Your real score for this exam sheet is $lastscore.)
  !endif
 !else
  Your best score for this exam is $lastscore. (It is the best score 
  which will be taken into account.)
 !endif
 <p>
!set test=!recordcnt wimshome/$wims_sesdir/examscore.$exam
!if $test>=2
 !href cmd=reply&job=scorereg Finish this exam session.
&nbsp;&nbsp;
!endif
 !href cmd=reply&job=student Back to work on the exam.
!endif

!if $wims_user=supervisor
 <p><center>
 !href cmd=resume Supervisor's view of the exam.
 </center>
!endif

:end
!tail

!reset job

