!set wims_module_log=error: $error

!if not_supervisor=$error
 Désolé, mais seul l'enseignant enregistré d'une classe peut accéder à cette
 page.
 !exit
!endif

!if bad_class=$error
 Etrange, mais je trouve que votre classe n'est pas valide!
 !exit
!endif

!if too_many_users=$error
 Votre classe a trop de participants ($ucnt).
 L'enregistrement manuel des notes n'est possible que pour les classes ayant au plus
 $maxuser participants. Désolé.
 !exit
!endif

!msg $error

