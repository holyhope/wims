!if $result=success
 !set wims_class_log=Import $import from $nei
!endif

!distribute item 每日信息,\
	练习,作业页,考试,文件,参加者\
    into name_motd,name_exo,name_sheet,name_exam,name_doc,name_user

从
<em><font color=blue>$nei_institution</font></em> 的班级
!href cmd=reply&job2=def $nei_description
输入
<p>

!if $result=confirm
 !if dep iswordof $cfm
  !if $itype=sheet
   <b>警告</b>.
你要输入的作业页可能使用相邻班定义的练习.
   <p>
如果你的班级没有同样的练习, 输入的作业不一定能正常工作.
请仔细检查是否相邻班的练习在你的班里都有复本.
  !endif
  !if $itype=exam
   <b>警告</b>!!! 考试页是以作业页为基础的.<p>
仅当你与相邻班有完全相同的激活的作业页, 并且在两个班里有相同次序的情况下,
输入考试页才有意义.
   <p>
如果做不到这一点, 输入的考试页肯定不能正常工作.
  !endif 
  <p>
 !endif
 !if replace iswordof $cfm
  <b>警告</b>. 你将要把你班级已有的 $(name_$itype) 用输入资源取代.
在此操作以后不再能恢复. 
  <p>
 !endif
 <p><center><font size="+1">
 !href cmd=reply&job2=import2&import=$import&impmethod=$impmethod&confirm=yes 确认此操作
 &nbsp;
 !href cmd=reply&job2=import $wims_name_giveup
 </font></center> <p>
 !exit
!endif

!if $result=none
没有参加这帐户加入你的班级. 请查看你的班级是否满员了(或者服务器没有增加新用户的空间了).
 <p>
 !exit
!endif

成功转移
!if $itype!=user
 一个$(name_$itype).
 !if $itype iswordof sheet exam
  (输入的 $(name_$itype) 置为不激活状态, 所以你可以作修改.)<p>
 !endif
!else
相邻班的 $importedcnt 个参加者帐户:
 <p><tt>
 $imported
 </tt> <p>
!endif

!href $verif 检查结果
.&nbsp;
!href cmd=reply&job2=import 输入其它资源
.
