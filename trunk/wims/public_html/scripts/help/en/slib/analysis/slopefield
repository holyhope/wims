slib_lang_exists_tmp=yes

slib_title=Direction field (for example for differential equation system)

slib_out= Draw vector field of an autonome differential system x' = f1 , y' = f2 \
  where f1 and f2 are expressions in x and y.
  
slib_comment=In case of js, may give the number of the applet (write js=1 for example).
