!if $result=success
 !set wims_class_log=Import $import form $nei
!endif

!distribute item Bericht van de dag,\
	oefening,werkblad,overhoring,document,leerling\
	into name_motd,name_exo,name_sheet,name_exam,name_doc,name_user

Import van de klas
!href cmd=reply&job2=def $nei_description
 van
<em><font color=blue>$nei_institution</font></em>
<p>

!if $result=confirm
 !if dep iswordof $cfm
  !if $itype=sheet
   <b>Waarschuwing</b>.
   Het werkblad dat u probeert te importeren, kan oefeningen bevatten
   afkomstig van deze buurklas
   <p>
   Als een zelfde oefening niet in uw eigen klas bestaat, zal dit werkblad uiteraard
  niet naar behoren functioneren.
  Kontroleer dus goed of alle oefeningen die verwerkt zijn in het werkblad, ook
  daadwerkelijk aanwezig zijn in uw eigen klas!
  !endif
  !if $itype=exam
   <b>WAARSCHUWING</b>!!! Overhoringen of tentamens baseren hun inhoud op werkbladen.
 <p>
Het importeren/lenen van een digitale overhoring cq tentamen is dus alleen zinvol
als U de beschikking hebt over <b>exact</b> gelijke actieve werkbladen en in de gelijke volgorde
als in de "leenklas"  
   <p>
   Als dit niet voor de volle 100% het geval is, zal de resulterende digitale overhoring/tentamen
   zeker foutief zijn!
  !endif 
  <p>
 !endif
 !if replace iswordof $cfm
  <b>WAARSCHUWING</b>.U bent van plan een bestaande $(name_$itype) in uw eigen klas te vervangen
  door een "import" uit een buur-klas. 
  Er is geen herstel meer mogelijk na deze handeling, en uw eigen bestaande  $(name_$itype), 
  die afwijkt van de te importeren versie, zal dus voorgoed verloren zijn.
  <p>
 !endif
 <p><center><font size=+1>
 !href cmd=reply&job2=import2&import=$import&impmethod=$impmethod&confirm=yes Bevestigen
 &nbsp;
 !href cmd=reply&job2=import Annuleren
 </font></center> <p>
 !exit
!endif

!if $result=none
Er kon geen leerlingen account meer toegevoegd worden aan uw klas.
Controleer s.v.p. of uw klas misschien reeds vol is.
 <p>
 !exit
!endif
Geslaagde transfer
!if $itype!=user
 one $(name_$itype).
 !if $itype iswordof sheet exam
  (Het geimporteerde $(name_$itype) is inactief gemaakt, 
  zo dat U het naar eigen inzicht kunt modificeren)
  <p>
 !endif
!else
 $importedcnt leerlingen acconts van de "buur-klas" accounts:
 <p><tt>
 $imported
 </tt> <p>
!endif

!href $verif Controleer het eindresultaat
.&nbsp;
!href cmd=reply&job2=import Importeer ander materiaal
.
