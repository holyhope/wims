!! +exec+ +var+ +string+
$lst_tit hex$lst_titend$type_both
  $lst_alias tohex
  $(lst_syntax)hex $emph string$emphend</tt>
  $lst_mean 返回一个新串, 它把 $emph string$emphend 中的每个字符
都替换成 16 进制码.
  <p>
特别地, 不管 $emph string$emphend 的内容如何, 得到的结果
可被用作变量名的一部分 (只要它不太长).


