
<center><h3>Browsen per onderwerp</h3></center>

!if $parm=$empty
 Kies een van de volgende onderwerpen. <ul>
 !for d in $tot_dom
  <p><li>
   !set l_=$(dom_$d)
    !href module=$module&cmd=new&job=$job&parm=$d <b>$(name_$d)</b>
   !if $l_=$empty
    .
   !else
    $ :
    !for v in $l_
     !href module=$module&cmd=new&job=$job&parm=$v $(name_$v)
     &nbsp;
    !next v
   !endif
 !next d
 </ul>
!else
 In  afdeling  <i><font color=blue>$name</font></i>, 
 zijn de volgende topics aanwezig
 !href module=$module&cmd=new&job=subject andere afdelingen
.
 <p><table border=0 cellpadding=2 cellspacing=1>
 !for t in $list
  <tr>
  <td align=left nowrap><b>$t</b>
  !if $has_enc=yes
   <td nowrap><small>
   !href target=wims_ref module=adm/enc.en&dom=math&job=search&search=$t $name_L
   </small></td>
  !endif
  !set key=$t
  !for i in T,P,X,S
   !set cat=$i
   <td nowrap><small>
   !href $search_addr $(name_$i)
   </small</td>
  !next i
  !next t
 </table> <p>
!endif

