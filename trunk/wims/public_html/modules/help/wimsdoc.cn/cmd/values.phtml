!! +var+ +exec+ +string+
tit:values
type:$type_both
syntax:!values `fns' for `v'=`v1' to `v2' [step `st']\
$ !values `fns' for `v' in `v1,v2,v3,...'
mean:把变量 `v' 用 `v1' 与\
  `v2' 之间的值逐次替换后, 输出函数 `fns' 值的表. <p>\
  在第二种情形, 用表 `v1,v2,...' 里的值逐次替换.\
  `fns' 可以是用逗号分隔的函数表. <p>\
  这个命令也可用于递归. 如果函数 `fns' 包含名为\
  `last' 的变量, 这个变量将取上次算得的函数值.\
  而初始值可以放在 WIMS 变量 <tt>$$recursion_start</tt> 中.

