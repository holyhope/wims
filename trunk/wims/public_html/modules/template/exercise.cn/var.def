

# 变量声明文件.
#
# 只有那些有可能由用户输入的变量或者需要对方请求保存的变量
# 才需要声明.
#
# 每个不加注解的行都定义一个变量. 每行的格式为:
#
#name   allow
#
# 或精简形式
#
#allow: name name name ...
#
#name: 变量名
#allow: 设为 allow 是指用户可以覆盖定义的值.
#       它也可取为 any(任意), deny(不可以), init(初始), config(配置), reply(回答).
#
#
#name   allow

status  deny

x   reply
y   reply

