!if $wims_read_parm=help
 !if $lang=cn

把制表符替换成换行符. 必要时可用于 applet.

<p><b>例:</b>

<pre>
\statement{ 
  \special{tabs2lines \texte	\texte}
}
</pre>

  !exit
 !endif

Convert tabulation in new line. Can be used for applet, if necessary.

<p><b>Example:</b>

<pre>
\statement{ 
  \special{tabs2lines \texte	\texte}
}
</pre>

 !exit
!endif

!translate $	$ to $\
$ in $wims_read_parm
