!! +exec+ +file+
tit:fileappend
type:$type_exec
alias:appendfile
syntax:!fileappend `fname content'
mean:把 `content' 添加在文件 `fname' 后面.\
  `fname' 是位于模块目录的`可写入' 子目录里的文件. <br/>\
  在 `content' 末尾将加一个换行符.
