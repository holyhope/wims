slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire suivant la loi logistique

slib_out=Simulation de <span class="tt">m</span> réalisations d'une variable aléatoire suivant la loi logistique.\
Si <span class="tt">m</span> est un vecteur à deux éléments entre crochets <span class="tt">[r,s]</span>, \
renvoie une matrice à <span class="tt">r</span> lignes et <span class="tt">s</span> colonnes.
slib_comment=La loi logistique a pour densité x -> exp(-x)(1+exp(-x))^(-2). 

