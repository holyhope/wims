!!! fichier des énoncés types !!!
:
<center>
<table bgcolor=lightblue width=80%>
  <tr>
    <td>
      On considère l'objet défini par <code>\obj</code>.
      <p style="background-color:moccasin">
        Dans la liste ci-dessous, cochez la (ou les) propriété(s) 
	vérifiée(s) par cet objet.
      </p>
    </td>
  </tr>
</table>
</center>

:
<center>
<table bgcolor=lightblue width=80%>
  <tr>
    <td>
      On considère l'objet défini par <code>\obj</code>.
      <p style="background-color:moccasin">
        Calculer la valeur de la fonction \fonc pour l'objet <code>\obj</code>.
      </p>
    </td>
  </tr>
</table>
</center>

:
<center>
<table bgcolor=lightblue width=80%>
  <tr>
    <td>
      On considère l'objet défini par <code>\obj</code>.
      <p style="background-color:moccasin">
        Dans la liste ci-dessous, cochez la (ou les) propriété(s) 
	vérifiée(s) par cet objet et calculer la valeur de la 
	fonction <code>\fonc </code> sur <code>\obj</code>.
      </p>
    </td>
  </tr>
</table>
</center>
