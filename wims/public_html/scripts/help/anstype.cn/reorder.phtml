

 <b>重排给出的对象.</b>
 (名称: <tt>reorder</tt>)
 <br>
用好的次序排列给出的对象.
 <br>
对象可以是文字(词或词组)、公式(使用 \(...) 的格式)或图形. 
标准答案应该用正确的次序列举它们, 每个对象作为一项.
服务器把这些对象随机乱排后显示给用户.
If there is a second line in the good reply, it will seperate the objects in the answer.
<p>
 这是以DynAPI为基础的, 可能在有的浏览器里不能正常工作.


