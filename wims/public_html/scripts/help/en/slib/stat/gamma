slib_lang_exists_tmp=yes

slib_title=Generation of Gamma random data


slib_out=Random data of <span class="tt">m</span> reals following a Gamma law with parameters <span class="tt">a</span>> 0 and <span class="tt">b</span> > 0.\
If <span class="tt">m</span> is a list <span class="tt">[r,s]</span>, it returns an <span class="tt">r</span> by <span class="tt">s</span> matrix. 
slib_comment=The Gamma density at x is b*exp(-b*x)*(b*x)^(a-1)/gamma(a) for x>=0
