
!read adm/title.phtml 3\
\
$title
<center>
!href cmd=reply&job2=def $nei_description
 van
<em><font color=blue>$nei_institution</font></em>
($wims_name_nameteacher:
<a href="mailto:$nei_email?subject=Class neighboring 
relations">$nei_supervisor</a>)
</center>
<p>
De klas <em><font color=blue>$nei_description</font></em>                                                                           
staat toe het onderstaande lesmateriaal te delen.
<small>
!href cmd=help&special_parm=share $wims_name_help
</small>
<ul>
!for r in $nsharable
 <li><p><em>$(name_$r)</em> ($(it_$(r)cnt) beschikbaar).
 !if $r isitemof $sharing or $(sharable_$r)!=$empty
  !if $r isitemof $sharing
   !if $(sharing_$r)=$nei
    U bent reeds aan het lenen/delen met hen.
    !href cmd=reply&job2=stopshare&stoprec=$r $name_stopsharing
. <small>
    !href cmd=help&special_parm=stopshare $wims_name_help
    </small>
   !else
   U kunt dit niet delen, 
   omdat dit materiaal niet van Uw eigen klas is.
   !endif
  !else
    U kunt dit niet delen,
     omdat dit al wordt gedeeld.
    Cascaded sharing is not supported. 
    So you must stop allowing these
   shares before you can share from others.
  
  !endif
 !else
  !if $(me_$(r)cnt)>0 and $r isitemof $nosharelist
   Dit kunt U niet delen omdat uw eigen $(name_$r) niet leeg is.
   Voor dat er gedeeld kan worden, moet u eerst uw eigen materiaal
   !href cmd=reply&job=clean opschonen
   (indien mogelijk).
  !else
   !href cmd=reply&addshare=$r Delen
.
   !if $(me_$(r)cnt)>0
    <b><font color=red>$wims_name_warning</font></b>.
    !if $r=sup
     Het delen van een supervisor account is niet verstandig ! 
     !href cmd=help&special_parm=supshare Meer Info.
    !else
     Dit zal $(name_$r) in deze klas kompleet wissen! 
     En het zal voorgoed verloren zijn als er geen backup is gemaakt.
    !endif
   !endif
  !endif
 !endif
!next r
</ul>

