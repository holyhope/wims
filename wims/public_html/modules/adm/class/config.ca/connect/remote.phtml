Remote server: 
<font color=blue>$connident</font>
($ident_desc) located at
<a href="http://$site">$site</a>, of type
<font color=blue>$ident_type</font>.

!if $cn_status!=OK
 <p><b>Error</b>.
 !if $cn_status=$empty
  The remote server does not reply. Please report to the
  !mailurl $wims_site_manager site manager
.
 !else
  The connection is rejected by the remote server. Error message:
<pre>
$cn_data
</pre>
 !endif
 !set error=yes
 !exit
!endif

