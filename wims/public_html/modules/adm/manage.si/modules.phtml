
!if $job2=one
 !read mu/one.phtml
 !reset auto muauto
 !exit
!endif

!if $job2=all
 Your $auto command is scheduled for execution. The result will be sent to
 you by email.
 <p>
 Please do not repeat this command before the execution is completed.
 <p> <hr width=70%> <p>
 !set job2=first
 !reset auto muauto
!endif

!form reply
<input type=hidden name=muauto value=check>
<center>
<table border=0>
<tr><td align=right>
<input type=submit value=Search> in module publication:
<td><input size=40 name=musite value="$musite">
<tr><td align=right>for modules in zone:
<td><input size=40 name=muzone value="$muzone">
<tr><td align=right>and category:
<td><input size=40 name=mucat value="$mucat">
<tr><td align=right>changed at least
<td><input size=4 name=mudelay value="$mudelay"> days ago
$table_end
</form>

<p> <hr width=70%> <p>

You need to
!href cmd=reply&job=mkindex rebuild module index
 after module update.
!href cmd=reply&job=conf&confcat=6 Click here
 to configure daily automatic module update.

!if $auto=check
 !read mu/$auto.phtml
!endif

!reset muauto
