slib_lang_exists_tmp=yes

slib_title=Generation of Fisher random data

slib_out=Random data of <span class="tt">m</span> reals following the Fisher law with parameters <span class="tt">a</span> and <span class="tt">b</span>\
 (two positive integers).\
If <span class="tt">m</span> is a list <span class="tt">[r,s]</span>, it returns an <span class="tt">r</span> by <span class="tt">s</span> matrix. 
