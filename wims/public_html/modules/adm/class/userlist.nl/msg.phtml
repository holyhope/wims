
!set wims_module_log=error: $error

<b>FOUT</b><br>.

!if not_supervisor=$error
Helaas, deze handeling is alleen voor de supervisor van een klas bedoeld.
 
 !exit
!endif

!if bad_class=$error
Vreemd, ik zie hier dat uw klas niet geldig is!
 !exit
!endif

!if bad_classpass=$error
Helaas, U hebt niet het juiste wachtwoord ingevuld.
U kunt dus de student-info niet wijzigen. 
 !exit
!endif

!if no_login iswordof $error
De opgestuurde data moeten wel een kolom bevatten met de 
loginnaam van de desbetreffende student/deelnemer.
 !exit
!endif

!if bad_filename iswordof $error
Uw opgestuurde file lijkt niet op spreadsheet-data in textformaat

:csvformat

S.V.P. bewaar Uw spreadsheet-data als een text-formaat 
(dus komma-separated of tab-separated lijsten,met de extensie *.csv *.tsv of *.txt)
 voor dat U ze naar deze WIMS-server stuurt.

 !exit
!endif

!if binary_upload iswordof $error
Uw spreadsheet data zijn in binair formaat en wordt niet geaccepteerd.
 
 <p>
 !goto csvformat
!endif

!if no_manual iswordof $error
 eerst maakt U een 
 !href module=adm/class/gradeshandmatige
 &nbsp;cijfer kolom voordat u er data naar toe kunt sturen.

 !exit
!endif

!if not_secure iswordof $error
Helaas, spreadsheet-data upload/download zijn alleen beschikbaar vanaf
de zogenaamde "secure hosts".
Ga naar de configuratie pagina van Uw klas om deze instellingen te bekijken, en eventueel aan te passen.
 !exit
!endif

!msg $error

