!if $wims_read_parm=help
 !if $lang=cn
写入预格式的参数时不执行赋值.

<p><b>例:</b>
<pre>\text{texte=
for a in \B
   a = a + 1
endfor
}
\statement{ \special{expandlines \texte}}
</pre>
 !exit
 !endif
Write parameter in pre style without evaluation.

<p><b>Example:</b>
<pre>\text{texte=
for a in \B
   a = a + 1
endfor
}
\statement{ \special{expandlines \texte}}
</pre>
 !exit
!endif

!translate $	$ to $\
$ in <pre>$wims_read_parm</pre>
