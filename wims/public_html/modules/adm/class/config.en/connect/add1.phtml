
!read adm/title.phtml 3\
\
Afegir una connexió a servidor

!read connect/remote.phtml

!if $error!=$empty
 !exit
!endif

!form reply
<input type=hidden name=job2 value=add2>
Per connectar-se a una classe en el servidor de dalt, cal especificar:
<p>
Identificador de la classe remota:
<input size=20 name=addclass>
<br><small>Si la classe remota està buida, el servidor crearà
una nova classe al servidor remot, duplicant les propietats d'aquesta.
</small>
<p>
Actualitzar automàticament la classe remota per als canvis realitzats en aquesta:
!formselect connupd1 list 0,1 prompt no,sí
<br>
Permetre que la classe remota modifiqui aquesta:
!formselect connupd2 list 0,1 prompt no,sí

<p><center><input type=submit value=OK></center>
</form>

!reset addclass

