
<h1 class="wims_docu_title">$tit</h1>

!if $error!=$empty
 !read docu/msg.phtml
 <p><hr> <p>
!endif

!if $edtype=file
 !set wims_form_method=file
!else
 !set wims_form_method=post
!endif
!form reply
<input type=hidden name=doc value="$doc">
<input type=hidden name=block value="$block">

!if $titb=$empty
$name_blockcreation
!else
$name_blockedition
!endif

!if $edtype=file
 !href cmd=reply&edtype=textarea [$name_compose]
!else
 !href cmd=reply&edtype=file [$name_file]
!endif
<br>

!set titb=!reinput $titb
$wims_name_title: <input size=50 name=Titb value="$titb"> <br>
!if $edtype=file
 Inserisci un file di testo: <input type=file size=35 name=wims_deposit>
 !if $titb!=$empty
  <br>Questo file
  !formselect addtype list replace,append,prepend prompt\
	sostituir&agrave;, sar&agrave; aggiunto in fondo a, sar&agrave; aggiunto in testa a 
  il documento esistente.
 !endif
 <br>
!else
 !mexec scripts/textarea.sh
!endif
<center>
<input type=submit name=preview value="$n_preview">
<input type=submit name=rEg value="$wims_name_tosave">
<input type=submit name=abandon value="$wims_name_giveup">
</center> <br>
!! Key words: <input size=50 name=Keyw value="$keyw"> <br>
!if $block!=main
 Blocco precedente: <input size=12 name=Prev value="$prev">
 successivo: <input size=12 name=Next value="$next">
 superiore: <input size=12 name=Upbl value="$upbl">
 <br>
!endif
Valido (<small>aaaammjj</small>): dal
 <input size=8 name=Dat1 value="$dat1"> al
 <input size=8 name=Dat2 value="$dat2">
</form>
!if $edtype=file
 <p><b>NOTA</b>. Formati file accettati:
 formato documenti WIMS, file html o LaTeX. Parti di file html o LaTeX
 possono essere accettati purch&eacute; siano formattate correttamente
 (in particolare non venga tagliato a met&agrave; un tag html, una
 formula matematica, e cos&igrave; via).
!endif
<p><hr> <p>


