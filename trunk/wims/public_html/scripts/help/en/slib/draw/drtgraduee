slib_lang_exists_tmp=yes

slib_title=Graduatied line
slib_parms=7\
600,image width\
-5,minimal abscissa\
5,maximal abscissa\
1,size of abscissa graduation step 1 0.1 ou 0.01\
black,axes colour\
grey,graduation colour\
40,margin

slib_out=Data for insdraw.

slib_comment=Specifically, the result is: by default, the flydraw code; \
the address of the prduced image if the option word <span class="tt">url</span> exists; \
the html code to insert the image in this case is already inserted with the option word<span class="tt">html</span>.