# CheckSheet
# permet de verifier l'existence de la feuille qsheet dans la classe qclass

#Pour commencer, on verifie la classe
!read scripts/check.class
!if $error!=$empty
 !exit
!endif

type=sheets
!read scripts/check.proc