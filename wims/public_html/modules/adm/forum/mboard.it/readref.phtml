 <small>
 !if $dcnt>1
  [
  !if $dlast!=$empty
   !href cmd=reply&job=read&r_msg=$dlast &lt;&lt; messaggio prec.
   &nbsp;
  !endif
  !href cmd=reply&job=list Indice per data
  !if $dnext!=$empty
   &nbsp;
   !href cmd=reply&job=read&r_msg=$dnext messaggio succ. &gt;&gt; 
  !endif
  ]
 !endif
 !if $r_prec!=$empty
  &nbsp;
  !set t_=!translate / to , in $r_prec
  !href cmd=reply&job=read&r_msg=$r_prec Precedente
 !endif
 !if $repcnt>0
  &nbsp;
  !href cmd=reply&job=read&r_msg=$r_rep1 Prima risposta
  &nbsp;
  !href cmd=reply&job=thread&th_orig=$l_thread&th_start=$r_msg Tutte le risposte
 !endif
 !if $thtcnt>1
  &nbsp;&nbsp;[
  !if $tlast!=$empty
   !href cmd=reply&job=read&r_msg=$tlast &lt;&lt;
   &nbsp;
  !endif
  !href cmd=reply&job=thread&th_orig=$l_thread&th_start=$l_thread Argomento
  !if $tnext!=$empty
   &nbsp;
   !href cmd=reply&job=read&r_msg=$tnext &gt;&gt;
  !endif
  ]
 !endif
 </small>

