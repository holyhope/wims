slib_lang_exists_tmp=yes

slib_title=Applet Jmol
slib_require=jmol
slib_parms=7\
,nom du fichier ou code décrivant la structure\
500,taille en x (en pixels)\
400,taille en y (en pixels)\
white,couleur\
,script Jmol (cf <a href="http://chemapps.stolaf.edu/jmol/docs/" target="wims_external">la documentation de Jmol</a> pour plus de détails)\
0,numéro de l'applet\
HTML5,type

slib_comment=La première applet d'une page doit impérativement avoir le numéro 0. \
Lorsque plusieurs applets Jmol sont insérées dans une page, elles doivent toutes avoir\
 des numéros différents.<br />\
Le fichier de la structure doit être de préférence dans le répertoire data du module \
ou dans le cas d'une classe dans le répertoire image (dans ce cas l'adresse doit être \
 de la forme <code class="wims_code_words">\imagedir/nom_du_fichier</code>). <br />\
Il est possible de charger une structure à partir de la base de données \
<i>National Cancer Institute</i>, en spécifiant soit le nom IUPAC soit le code SMILES \
de la structure. Dans ce cas le nom doit être précédé d'un @. Les exercices utilisant\
 cette possibilité seront dépendents de l'accès à cette base.
slib_out=code permettant d'afficher l'applet Jmol
