!set email=!trim $email
!set author=!trim $author

!if $author!=$empty
 Auteur de cet exercice&nbsp;:
 !if $email!=$empty
  !mailurl $email $author
.
 !else
  $author.
 !endif
!endif
!if $solution!=$empty and $hint!=$empty
 Cet exercice contient à la fois une solution expliquée et une indication.
!endif
!if $solution!=$empty and $hint=$empty
 Cet exercice contient une solution expliquée mais pas d'indication.
!endif
!if $solution=$empty and $hint!=$empty
 Cet exercice contient une indication mais pas de solution expliquée.
!endif
!if $solution$hint=$empty
 Cet exercice ne contient ni solution expliquée ni indication.
!endif
Lien pour cet exercice : <br>
!set exolink=$wims_ref_name?module=$module&cmd=new&exo=$thisexo
<a href=$exolink>$exolink</a> <br>
Lien pour la config actuelle : <br>
!if $module_init_parm!=$empty
 !set exolink=$wims_ref_name?module=$module&cmd=new&$module_init_parm
!else
 !set exolink=$wims_ref_name?module=$module&cmd=new
!endif
<a href=$exolink>$exolink</a>
<p>
Information concernant cette collection d'exercices. <p>
$module_about
<p>
Les droits d'auteur des exercices appartiennent à leurs auteurs respectifs.

!changeto NEWS.phtml
