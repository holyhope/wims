!if $cmd=help and $help_subject iswordof conf soft and $wims_ismanager=2
 !changeto conf/help-$help_subject.phtml
!endif

!header

!if $wims_ismanager iswordof -1 1
 This tool allows site manager to maintain this WIMS installation online. Its
 access is strictly controled by definitions in the configuration file
 <tt>log/wims.conf</tt>. <p>
!endif

!if $job!=$empty
 !read $job.phtml
!endif
!if $wims_ismanager=2 and $job!=list
 <p><center>
 !href cmd=reply&job=list Other maintenance tasks
 </center> <p>
!endif

!tail

!reset auth,save,abandon
