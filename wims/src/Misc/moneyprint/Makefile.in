# @configure_input@

wims_home=../../..
cc=@CC@
cflags=@CFLAGS@ -Wall
defines=@DEFINES@
lopt=-lm

LDFLAGS =

all: moneyprint

clean:
	rm -f *.o *.core *.stackdump moneyprint

moneyprint: moneyprint.c
	$(cc) $(cflags) $(LDFLAGS) -o moneyprint moneyprint.c
