
!if $directcnt>0
 Pots canviar directament a:<ul>
 !for cl in $directlist
  !distribute words $cl into c_,u_
  !set n_=!defof class_institution, class_description in\
	wimshome/log/classes/$c_/.def
  <li>
  !if $u_=supervisor
   !set t_=supervisor
  !else
   !set t_=$type
  !endif
  !href cmd=reply&type=auth$t_&class=$c_&auth_user=$u_ $n_
  (<tt>$u_</tt>)
 !next cl
 </ul>
 <p><hr> <p>
 !if $wims_user=supervisor
  O triar el nom $name_level_else $sousclasses_else a la taula següent.
 !endif
!else
 !if $wims_user=supervisor
  Per anar a una altra $name_level_else $sousclasses_else, tria el seu nom a
 la taula següent.
 !else
  No tens compte en altres classes.
 !endif
!endif

