<center>$classname<p><h1>$cdt_title.</h1>
 <font size=+1><b>Work to do
 !if $jour_begin!=$jour_end
  between <font color=green>$jyear/$jmonth/$jday</font> and <font color=green>$jyear2/$jmonth2/$jday2</font>
 !else
  for  <font color=green>
  $jyear/$jmonth/$jday</font>
 !endif
</b></font>
</center> 
<p><p>

!if $nbligne=0
 There is no work to do for the $jyear/$jmonth/$jday.
 <p>
 !goto end
!endif
 <center>
 !let $table_border=1
 $table_header
 $table_hdtr 
  <th>For</th>
  !if no isin $cdt_supervise
   <th>Mati&egrave;res</th>
  !endif
  <th>Work</th><th>Give the</th>
 </tr>
 !for i=1 to $nbligne
  $table_tr
   !let don=!line $i of $list
   !distribute item $don into todo,date,dir,matiere
   !let m_filedir=$wims_ref_name?session=$wims_session&cmd=getfile&special_parm=cdt/$dir
   !read wimshome/log/classes/$dir/cdt/def/$date 
   !let no=!positionof item $todo in $date_todo
   <td>
    !read proc/date.phtml $todo 
   </td>
   !if no isin $cdt_supervise
    <td>$matiere</td>
   !endif
   <td valign=top>
    !read wimshome/log/classes/$dir/cdt/def/$date todo$no
   </td>
   <td>
    !read proc/date.phtml $date
   </td>
  </tr>
 !next i
 $table_end
:end
<p><p><p>
<a href="javascript:window.print()">Print</a> &nbsp; 
<a href="javascript:window.close()">Close window</a>
 </center>

