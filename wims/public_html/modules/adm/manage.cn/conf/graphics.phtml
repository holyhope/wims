
!if $save!=$empty and $changed>0
 !href cmd=reply#check 点击这里
以检验新设置的效果.
 <p>
 !exit
!endif

<p><hr><p>
<a name=check>
!if new.png notin $cvtest
 <b>错误</b>! 本系统里没有发现运转中的ImageMagick.
 没有这个软件包, 大部分WIMS图形以及动画都将受损.
 因此在继续进行以前, 建议你一定要把ImageMagick安装好.
 <p>
 !exit
!endif
!distribute char $useropts into us,up
!if $us!=$empty and $us>=0 and $us<=9
 !set wims_texsize=$[$wims_texbasesize-$us+1]
!endif

检验动态图形 (你可在不同的浏览器里执行):
<p>
!set t=1
!set ins_align=middle
以下公式
!set formula=\sqrt{x^2+y^2}=\sin(t^3)
!instex $$$$ $formula $$$$
使用了动态TeX插入. 应该能正确显示, 否则说明你的系统里没有正确安装TeX.
你可以改变TeX字体的大小等级使它们不会显得太大或太小
(请先阅读 
!href cmd=help&help_subject=conf&help_conf=texbasesize 帮助
).
<p>
以下两个图形由内涵的动态绘图程序生成.
一个是静态图形, 另一个是动画. 你能正确地看到这些图形吗?
如果不能, 请尝试别的绘图/动画格式.
<p><center>
<table border=0>
<tr><td>
!set insdraw_size=200,200
!insdraw fill 0,0,white\
transparent white\
xrange -1,1\
yrange -1,1\
disk 0,0,240,grey\
disk 0,0,200,red\
disk 0,0,160,cyan\
disk 0,0,120,green\
disk 0,0,80,blue\
disk 0,0,40,yellow
</td><td>&nbsp;&nbsp;&nbsp;</td><td>
!set insdraw_size=200,200
!set ins_anim_frames=15
!set ins_anim_delay =0.3
!set ins_anim_loop  =0
!insdraw fill 0,0,white\
transparent white\
v=1.3\
xrange -v,v\
yrange -v,v\
arrow -v,0,v,0,10,grey\
arrow 0,-v,0,v,10,grey\
linewidth 2\
levelcurve green,x^4+y^4+(cos(s*pi)*x-sin(s*pi)*y)*(sin(s*pi)*x+cos(s*pi)*y),0,-0.06,0.2,0.6,1
</td></tr>$table_end <p>

下面使用了gnuplot描图. 曲线应该有不同的颜色.
如果不能正确显示, 请尝试不同的图形格式.
<p><center>
!set ins_border=2
!set insplot_set=size 0.6,0.6
!set insplot_colors= xffffff x000000 x404040 \
 x$color_1 x$color_2 x$color_3 x$color_4 x$color_5 x$color_6 x$color_7 x$color_8 x$color_9 x$color_10
!insplot [-5:5] [-1.4:1.4] sin(x),cos(x),-sin(x),-cos(x)
</center> <p>
以下的光线跟踪图形不受上述图形格式的影响. 如果没有出现图形, 那么一定是
!exec povray about
 没有正确安装.
 !href cmd=help&help_subject=soft&help_conf=povray 帮助

<p><center>
!set povray_width=200
!set povray_height=200
!set povsrc=!record 0 of conf/pov.src
!set rayout=!exec povray $povsrc
!line 1 of $rayout
</center> <p>

