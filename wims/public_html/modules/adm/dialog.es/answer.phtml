
<a name=ans>
!if $badask=yes
 !if $comment!=$empty
  Su pregunta $badname es ilegal:
  $comment 
 !else
  Su pregunta $badname es ilegal, esa información no tiene sentido.
 !endif
 <p>Su trabajo en este ejercicio ha terminado con esta pregunta ilegal.
 !exit
!endif

!if $speculate=yes
 ¡No tiene suficiente información para resolver el problema! Por lo tanto,
 su respuesta no es válida, y se toma como si fuera falsa.
 !exit
!else
 !read $deffile ans
!endif

!if $solution!=$empty and ($presentsol>1 or ($presentsol=1 and $thisscore<0.7))
 <p><b>Solución.</b> $solution
!endif

<p>
!if $gotcnt>$minask
 Podría haber resuelto el problema haciendo únicamente $minask
 !if $minask>1
  preguntas.
 !else
  pregunta.
 !endif
!endif

!if $uselesscnt>0
 Las siguientes informaciones son inútiles: <b>$useless</b>.
!endif

