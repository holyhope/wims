slib_lang_exists_tmp=yes

slib_title=Conversion de coordonnées [pixels] <-> [repère mathématique]
slib_parms=10\
,x\
,y\
,taille horizontale de l'image (en pixels)\
,taille verticale de l'image (en pixels)\
,valeur min pour x dans le repère mathématique\
,valeur max pour x dans le repère mathématique\
,valeur min pour y dans le repère mathématique\
,valeur max pour y dans le repère mathématique\
0,bordure\
,mot d'option : <tt>pixels</tt>

slib_out=Conversion de pixels en coordonnées dans un repère mathématique (option pixels) \
ou du repère vers les pixels (sans mot d'option)
