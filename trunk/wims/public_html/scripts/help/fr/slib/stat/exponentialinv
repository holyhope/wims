slib_lang_exists_tmp=yes

slib_title=Quantile de la loi exponentielle

slib_out=Valeurs en <span class="tt">data</span> des quantiles de la loi exponentielle de paramètre\
 <span class="tt">lambda</span> (positif).
slib_comment=<span class="tt">data</span> peut être une liste de nombres entre 0 et 1.\
La densité en un réel x positif est lambda*exp(-lambda*x)

