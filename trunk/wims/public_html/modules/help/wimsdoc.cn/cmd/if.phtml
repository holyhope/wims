!! +exec+ +jump+
tit:if
type:$type_exec
syntax:!if `string1 relation string2'\
  ... (多行内容)\
 $ !endif
mean:条件分叉: 当 relation' 满足时,\
  就执行 `!if' 与 `!endif' 中间的多行内容.\
  `string1' 与 `string2' 前后的空格在比较前就被自动清除.
