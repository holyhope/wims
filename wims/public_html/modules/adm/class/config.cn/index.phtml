
!if $job2=structure
 !set tit=更新分班索引
!endif
!if $job2=userlist
 !set tit=更新参加者索引
!endif
!if $job2=teacherlist
 !set tit=更新教师索引
!endif
!if $job2=oef
 !set tit=更新练习索引
!endif
!if $job2=doc
 !set tit=更新文件索引
!endif

!read adm/title.phtml 3\
\
$tit


完成.
!if $result != $empty
输出:
<pre>
$result
</pre>
!endif
