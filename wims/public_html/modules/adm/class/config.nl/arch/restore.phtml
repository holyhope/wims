
De klas is hersteld met de backup: $restored.

!if $user_bad!=$empty
 <p>
 Het herstel van de leerling accounts (en score data)
 is niet gelukt omdat er te weinig ruimte is in de virtuele klas.
!endif

!if $exp_bad!=$empty
 !distribute items onherkenbaar, te vroeg, te laat into ebad,eearly,elate
 <p>
 De verloopdatum van deze klas is gewijzigd, omdat de hersteldatum $(e$exp_bad) is. 
!endif

