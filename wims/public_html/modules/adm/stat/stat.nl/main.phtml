!!<html><head>
!!$wims_html_header
!!</head>
!!<body $wims_htmlbody>
!!!headmenu
!header1

!if $cmd=help and $special_parm=about
 !read about.phtml
 !goto end
!endif

!if $cmd=help
 !read help.phtml
 !goto end
!endif

<h1 class="wims_title">WIMS op $httpd_HTTP_HOST
<br>Server Statistieken</h1>
<p><hr> <p>

!if $sitestart=$empty
    Helaas, maar op deze server zijn de statistieken niet openbaar. <p>
 !goto end
!endif

!if $show=yes and $total_site>0 and $laps>0
!!if $show=yes
 !href cmd=resume Terug
 </a> naar het menu
 <p>
 !read answer.phtml
 !href cmd=resume Terug 
 </a> naar het menu
 <p>
!else
 !if $usercnt>0
  <p>
 !endif
 !read menu.phtml
!endif
:end
!homeref
!tail

!set stat_module=
