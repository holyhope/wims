# CheckExam
# permet de verifier l'existence de l'examen qexam dans la classe qclass

#Pour commencer, on verifie la classe
!read scripts/check.class
!if $error!=$empty
 !exit
!endif

type=exams
!read scripts/check.proc

