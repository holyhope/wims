slib_lang_exists_tmp=yes

slib_title=Simplify a linear combination

slib_parms=2\
, numerical list inside brackets\
, symbol list of variables inside brackets\

slib_out=a1 V1+a2 V2+  
slib_comment= simplify the expression (if a1=0, negative); the numbers of items is bounded by 25.