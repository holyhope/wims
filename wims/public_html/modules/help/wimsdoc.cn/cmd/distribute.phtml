!! +exec+ +vardef+
$lst_tit distribute$lst_titend$type_exec
  $(lst_syntax)distribute $emph type string$emphend into
  $emph namelist$emphend</tt> <br>
  $lst_mean 把 $emph string$emphend 的内容分配给变量 $emph namelist$emphend. 
  根据 $emph type $emphend 的取值(可以是 words, items, lines, chars),
  分配按词、按项目、按行或按字符进行. <p>
  例如, 如果 $emph type$emphend 是 <tt>words</tt>, 
  则 $emph namelist$emphend 里的第一、第二...个变量将依次被设置为
  $emph string$emphend 里的第一、第二...个词.
  
