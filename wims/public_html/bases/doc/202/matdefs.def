!set titb=<b>Diverses définitions sur les matrices</b>.
!set keyw=matrice ligne, matrice colonne, matrice triangulaire, diagonale
!set datm=20011130
!set prev=
!set next=matalg
!set upbl=test2
!set dat1=19000101
!set dat2=24000101

!if $wims_read_parm!=$empty
 !goto $wims_read_parm
!endif

!exit

:content
<p>
<a name=matrice_colonne>
<a name=matrice_ligne>
<a name=matrice_carree>
Une matrice $m_m×$m_n A est une <em>matrice-ligne</em> si $m_m=1, ou <em>matrice-colonne</em> si $m_n=1. A est une <em>matrice carrée</em> si 
$m_m=$m_n.
<p>
<a name=diagonale>
Dans une matrice A=($m_a<sub>$m_i$m_j</sub>), les coefficients $m_a<sub>11</sub>, $m_a<sub>22</sub>, $m_a<sub>33</sub> ... sont des coefficients <em>diagonaux</em>. A est une matrice <em>diagonale</em> si tous ses coefficients non-diagonaux sont nuls.
<p>
<a name=triangulaire>
La matrice A=($m_a<sub>$m_i$m_j</sub>) est <em>triangulaire supérieure</em>
(resp. <em>triangulaire inférieure</em>) si tous ses coefficients au-dessous (resp. au-dessus) de la diagonale sont nuls, c'est-à-dire $m_a<sub>$m_i$m_j</sub>&nbsp;=&nbsp;0 si $m_i &gt; $m_j (resp. si $m_i &lt; $m_j).
<p>
<a name=trace>
La <em>trace</em> d'une matrice A=($m_a<sub>$m_i$m_j</sub>) de dimension $m_m&times;$m_n est la somme des coefficients diagonaux&nbsp;&#58;
<p><center>
  trace(A) = 
!insmath \sum_{i=1}^{\min(m,n)}a_{ii}

</center><p>
<b>Remarque</b>. Bien que les définitions de diagonale, triangulaire et trace sont valables pour les matrices de dimension arbitraire, en générale elles ne sont intéressantes que pour les matrices carrées.
