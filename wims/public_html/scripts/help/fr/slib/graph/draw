slib_lang_exists_tmp=yes

slib_title=Dessin de graphe
slib_parms=6\
,matrice de 0 et de 1 (booléenne)\
blue,couleur des arcs\
blue,couleur des sommets\
[1,2,..],noms des sommets\
,chemins à dessiner avec une couleur différente, optionnel\
raw,choix de l'option : <tt>url</tt>, <tt>html</tt> ou <tt>raw</tt>

slib_out=Dessin du graphe (+ taille de l'image si l'option est \
<tt>url</tt> ou <tt>html</tt>).

slib_comment=la matrice du graphe doit être entre crochets; si les couleurs sont définies par trois nombres compris entre 0 et 255,\
 mettre ces nombres entre parenthèses;\
 Le premier élément du résultat est : par défaut le code flydraw; \
l'adresse de l'image générée si l'option est <tt>url</tt> ; \
 le code HTML pour insérer l'image est déjà inséré pour l'option <tt>html</tt>.

