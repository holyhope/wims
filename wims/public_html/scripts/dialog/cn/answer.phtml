!exit
<a name=ans></a>
!if $badask=yes
 !if $comment!=$empty
  你对$badname的请求是非法的:
  $comment 
 !else
  你对$badname的请求是非法的, 因为这个信息没有意义.
 !endif
 <p>由于这个非法的问题, 你对这个练习的工作被终止了.
 !exit
!endif

!if $speculate=yes
 你没有足够的信息解决此问题! 你的答复无效, 并被认为是错的.
 !exit
!else
 !read $deffile ans
!endif

!if $solution!=$empty and ($presentsol>1 or ($presentsol=1 and $thisscore<0.7))
 <p><b>解.</b> $solution
!endif

<p>
!if $gotcnt>$minask
 其实只要问 $minask 个问题就够了.
!endif

!if $uselesscnt>0
 以下信息是无用的: <b>$useless</b>.
!endif

