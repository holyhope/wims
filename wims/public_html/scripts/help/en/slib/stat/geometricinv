slib_lang_exists_tmp=yes

slib_title=Quantiles of a Geometric law on N

slib_out=Quantile at <span class="tt">data</span> of the geometric law on N with parameter <span class="tt">p</span>.
slib_comment=<span class="tt">data</span> can be a list of reals, the result should be less than M.\
 The density function is P( X = k ) = p(1-p)^k for every nonnegative integer k. 

