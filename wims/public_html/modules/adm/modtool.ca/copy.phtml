!if $error!=$empty
 !goto end
!endif

!if $originheader!=$empty and $job=move
 <b>ATTENTION</b> ! Renommer le module va casser les feuilles de
 travail qui l'utilisent. Assurez-vous que ce n'est pas le cas, ou
 faites plutôt une
 !href cmd=reply&jobreq=copy duplication
.
 <p>
!endif

!if $target=$empty
 !form reply
 <input type=hidden name=jobreq value="$job">
 !distribute item Copier,Renommer,Dupliquer,Renommer into P_copy,P_move,L_copy,L_move
 !if $originheader!=$empty
  $(L_$job) le module <em>$otit</em> (<tt>$original2</tt>) à <p>
 !else
  $(P_$job) le module public <em>$otit</em> (<tt>$original2</tt>) dans
  votre espace de développement, sous l'adresse <p>
 !endif
 !read scripts/addressform.phtml
 </form>
 !if $originheader!=$empty and $o1 isitemof $zonelist,local
  <p>
  <b>Remarque</b>. Renommer ou dupliquer un module d'adresse publique
  peut conduire à la duplication de sa publication. Soyez très prudent !
  <p>
  Si vous voulez faire des tests sur le module, mettez-le dans la zone test.
 !endif
 !if $originheader!=$empty and ~ notin $original2
  <p>
  <b>Remarque</b>. Si vous avez déjà publié ce module, veuillez changer cette
  adresse exactement selon son adresse publique. Sinon vous risquez
  une duplication de sa publication.
 !endif
!endif




:end
!if $original2!=$empty and $originheader=$empty
 !default otit=!module title $original2
 <p>
 !href module=$original2 Retourner à $otit
 <p>
!endif


