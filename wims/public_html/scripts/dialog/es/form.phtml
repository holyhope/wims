
!if $remcnt>0
 !set wims_ref_anchor=#infogot
 !form reply
 !if $gotcnt<1
  <p>Para resolver el problema, puede 
  <input type=submit value="hacer preguntas">
 !else
  <p>Ahora puede continuar
  <input type=submit value="haciendo preguntas">
 !endif
 acerca de $objname:
 !formselect ask list 0,$remlist prompt ?? Elija la pregunta ??,$prompts
 !if $comment!=$empty
  <p>$comment
 !endif
 <p></form>
 <small><b>Atención</b>. Su nota depende del número de preguntas que
 haga. Se aplicará una penalización si pide información irrelevante o
 inútil.</small> <p>
 <hr width=50%> <p>
!endif

!if $gotcnt<1
 !exit
!endif

!set wims_ref_anchor=#ans
!form reply
 !if $remcnt>0
  Cuando piense que tiene suficiente información para resolver el problema, 
 !else
  Ahora, por favor,
 !endif
 <input type=submit value="escriba la respuesta">&nbsp;:<p>
 !read $deffile form
</center>
</form>

