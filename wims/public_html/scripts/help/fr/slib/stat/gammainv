slib_lang_exists_tmp=yes

slib_title=Quantile of cumulative gamma distribution

slib_out=Quantile at <tt>data</tt> of the gamma distribution.
slib_comment=<tt>data</tt> can be a list of reals; density is b*exp(-b*x)*(b*x)^(a-1)/gamma(a) for x>=0

