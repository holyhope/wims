# @configure_input@

wims_home=../../..
cc=@CC@
cflags=@CFLAGS@ -Wall
defines=@DEFINES@
lopt=-lm

LDFLAGS =

all: scienceprint

clean:
	rm -f *.o *.core *.stackdump scienceprint

scienceprint: scienceprint.c
	$(cc) $(cflags) $(LDFLAGS) -o scienceprint scienceprint.c $(lopt)
