!set wims_module_log=error: $error

!if not_supervisor=$error
 Spiacente, ma solo il docente del corso pu&ograve; accedere a questa
 pagina.
 !exit
!endif

!if bad_class=$error
 Il corso selezionato non esiste.
 !exit
!endif

!if too_many_users=$error
 Il corso ha troppi iscritti ($ucnt).
 Spiacenti: l'inserimento manuale dei voti pu&ograve; essere effettuato solo
 per corsi con massimo $maxuser studenti. 
 !exit
!endif

!msg $error

