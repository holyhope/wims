slib_lang_exists_tmp=yes

slib_title=Balance (Roberval)
slib_parms=4\
,masses on the left\
,masses on the right\
0,slope\
,color of the objects\
raw,option words : one, url, html, raw

slib_out=Drawing of a Roberval balance
slib_comment=the output is: by default, the flydraw code; \
the address of the generated image if the option word is <tt>url</tt> ; \
the html code for inserting image is already inserted for the option <tt>html</tt>.\
If <tt>one</tt> is in the option words, the form of the object is a rectangle of fixed size.