!set titb=Structure d'un Document sous WIMS
!set keyw=
!set datm=20101204
!set prev=
!set next=compose
!set upbl=main
!set dat1=19000101
!set dat2=24000101

!if $wims_read_parm!=$empty
 !goto $wims_read_parm
!endif

!exit

:content
<p>
Un document est composé d'un ou plusieurs <em>blocs</em>. Un bloc
correspond à peu près à une page html, mais il est aussi possible d'insérer un ou plusieurs blocs dans un autre.
<p>
Chaque bloc doit avoir un nom (un mot unique) et un titre. Le point
d'entrée du document est un bloc sous le nom <tt>main</tt>. C'est ce
bloc qui conduira les lecteurs au contenu entier du document.
<p>
Pour créer un nouveau bloc (excepté le bloc d'entrée <tt>main</tt>), vous
devez d'abord le citer dans un bloc existant. Quand vous lisez ce dernier
bloc, la citation deviendra un lien qui vous conduit à la création du
nouveau bloc.
<p>
Il y a trois façons pour un bloc d'en citer un autre.<ol>
<li>Par un lien. 
!read primitives.phtml 1, link, link,La syntaxe du lien 

 est très simple&nbsp;&#58; il suffit de donner le nom du bloc cité.
 <p>
 On peut aussi utiliser des liens pour citer des blocs d'autres documents.
</li><li>Comme une partie pliable du bloc citant. Cette partie peut être
 dépliée ou repliée à volonté par le lecteur.
 
!read primitives.phtml 2, link, fold,La syntaxe 
.
</li><li>Comme une partie intégrante de la page du bloc citant. L'insertion
 est transparente au lecteur. 
!read primitives.phtml 3, link, embed, La syntaxe 
.
</li></ol>
<p>
