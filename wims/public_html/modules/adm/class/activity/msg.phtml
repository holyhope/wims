!set wims_module_log=error: $error

!if not_supervisor=$error
 $name_not_supervisor
 !!Désolé, mais l'opération de préparation / modification d'une feuille de travail est réservée aux enseignants enregistrés d'une classe.
 !exit
!endif

!if bad_class=$error
 $name_bad_class
 !!Etrange, mais je trouve que votre classe n'est pas valide!
 !exit
!endif

