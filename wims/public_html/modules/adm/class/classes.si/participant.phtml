
!if $clcnt+$sccnt+$directcnt<1
 Na tem stre¾niku trenutno ni nobene virtualne uèilnice v slovenskem jeziku. Morda i¹èete uèilnico v kak¹nem 
 !href module=home&list=0 drugem jeziku
?
 !exit
!endif

!if $wims_user!=$empty
 !read direct.phtml
 !if $wims_user!=supervisor
  !exit
 !endif
!else
S klikom na ime lahko vstopite v virtualno uèilnico.
!endif

!if $sccnt>0
 <p>$table_header
 <caption>Ustanove in skupine uèilnic</caption>
 $table_hdtr
 <th>Ime ustanove<th>Skrbnik<th>Status<th>Registracija
 !for i=1 to $sccnt
  !set l=!line $i of $sclasses
  !distribute items $l into code,expire,inst,cl,la,ty,lv,pswd
  !if $superclass=$empty
   !set show=$inst
  !else
   !set show=$cl
  !endif
  !set supv=!item 9 of $l
  !if $supv=$empty
   !reset class_password
   !defread wimshome/log/classes/$code/.def
   !set supv=$class_supervisor
   !set pswd=$class_password
  !endif
  
  $table_tr<td align=center>
  !href module=$module&type=authparticipant&class=$code $show
  </td> <td align=center>
  $supv
  </td> <td align=center>
  !if $pswd=$empty
   javno
  !else
   za¹èiteno
  !endif
  <td align=center>
  !href module=adm/class/reguser&class=$code&subclass=yes Registracija
  </td>
 !next i
 $table_end <p>
!endif

!if $clcnt>0
 <p>$table_header
 !if $superclass=$empty
  <caption>Samostojne uèilnice</caption>
 !else
  !set inst=!defof class_institution in wimshome/log/classes/$superclass/.def
  <caption>Uèilnice ustanove $inst</caption>
 !endif
 $table_hdtr<th>Ime uèilnice<th>Uèitelj<th>Stopnja<th>Status<th>Registracija
 !for i=1 to $clcnt
  !set l=!line $i of $classes
  !distribute items $l into code,expire,inst,cl,la,ty,lv,pswd
  !set supv=!item 9 of $l
  !if $supv=$empty
   !reset class_password
   !defread wimshome/log/classes/$code/.def
   !set lv=$class_level
   !set supv=$class_supervisor
   !set pswd=$class_password
  !endif
  !distribute char $lv into l_,y_
  
  !if $superclass=$empty
   !set show=$inst <br>$cl
  !else
   !set show=$cl
  !endif
  $table_tr<td align=center>
  !href module=$module&type=authparticipant&class=$code $show
  </td><td align=center>
  $supv
  </td><td align=center>$(n_$l_)<br> Leto $y_</td>
  <td align=center>
  !if $pswd=$empty
   javno
  !else
   za¹èiteno
  !endif
  </td><td align=center>
  !href module=adm/class/reguser?class=$code Registracija
  </td>
 !next i
 $table_end <p>
!endif

Novi uporabniki se s klikom na besedo 'Registracija' registrirate za uporabo uèilnice
(èe je uèilnica za¹èitena z geslom, potrebujete skupno geslo, ki vam ga posreduje va¹ skrbnik).
<p>
Èe ste se ¾e registrirali, pa se s klikom na ime uèilnice prijavite za delo v njej (potrebujete uporabni¹ko ime in osebno geslo, ki ste ju izbrali ob registraciji).

