slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire suivant la loi exponentielle.

slib_out=Simulation de <span class="tt">m</span> réalisations d'une variable aléatoire suivant la loi exponentielle \
de paramètre <span class="tt">lambda</span> (positif).
slib_comment= La densité en un réel x positif est lambda*exp(-lambda*x)

