
!if $fsize=$empty
 Ho sento, per un error intern no s'ha arxivat la teua classe.
 !exit
!endif

Aquí està l'arxiu de còpia de seguretat de la teva classe :
!getfile $fname.$format $fname.$format
 ($fsize K).
<p>

!if $fsize<50 and all isitemof $selection
 !exit
!endif

!form reply
<input type=hidden name=job value=$job>
<input type=hidden name=job2 value=$job2>
!if $fsize<200
 També podeu fer una
!else
 Aquest arxiu de còpia de seguretat té una mida que pot superar el límit permès
 per enviar al servidor. Per reduir la mida del fitxer, podeu fer
 un
!endif

<input type=submit value="Còpia de seguretat selectiva">:
<p>
!set selection=$select
!formcheckbox selection list $reclist prompt $tot
<p>
<b>Observació</b>. D'acord amb les activitats de la classe, és habitual 
el registre de les activitats dels participants, missatges de xat i dels
documents de la classe que formen el gruix de la mida de l'arxiu. Pots
fer diverses còpies de seguretat selectives dels diferents recursos.
</form>


