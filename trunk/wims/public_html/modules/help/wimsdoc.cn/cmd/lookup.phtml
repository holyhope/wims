!! +var+ +exec+ +string+
tit:lookup
type:$type_both
syntax:!lookup `defname' in `fname'
mean:返回文件 `fname' 内对词 `defname' 的定义,\
  文件 `fname' 必须是词典文件, 语法按程序 <tt>translator</tt>\
  的规定(不必排序).\
  <p>\
  `fname' 或者位于模块目录里(此时不必指明),\
  或者位于目录 bases/.
