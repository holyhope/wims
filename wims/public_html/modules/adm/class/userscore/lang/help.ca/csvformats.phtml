
!read adm/title.phtml 1\
\
Format per full de càlcul

csv (valors separats per comes) i tsv (valors separats per tabuladors) són formats
de text per a dades de fulls de càlcul. La majoria dels fulls de càlcul
les reconeixen.
<p>
ods is the spreadsheet format defined by the OpenDocument ISO standard. It is supported by LibreOffice and OpenOffice, and it should be supported by any fair spreadsheet program.
</p>

