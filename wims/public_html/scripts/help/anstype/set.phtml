 <b>Finite set (literal, formal and approximative).</b>
 (names: <tt>set</tt>, <tt>fset</tt> and <tt>aset</tt>) <br>
 This is a list of (literal) elements separated by commas, 
 where the order is not important.
 <p>
 
 for <tt>set</tt>, No evaluation is done to the elements (so that 2 and 1+1
 are not the same element).

 <p>
 For <tt>fset</tt>, the elements are evaluated as
 <em>formal expressions</em>.

 <p>
 For <tt>aset</tt>, the elements are evaluated as
 <em>approximative expressions</em>.

