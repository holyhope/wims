slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire suivant la loi de Cauchy.
slib_out=Simulation de <tt>m</tt> réalisations d'une variable aléatoire suivant la loi de Cauchy \
de paramètres <tt>lambda</tt> et <tt>sigma</tt>.\
Si <tt>m</tt> est un vecteur à deux éléments entre crochets <tt>[r,s]</tt>, renvoie une matrice à <tt>r</tt> lignes\
et  <tt>s</tt> colonnes.
slib_comment=La densité en x est (pi*sigma)^(-1)*(1+(x-lambda)/sigma)^(-2)