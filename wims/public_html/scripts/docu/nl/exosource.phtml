
<center><h1>$tit</h1></center>

!if $error!=$empty
 !read docu/en/msg.phtml
 <p><hr> <p>
!endif

!if $titb=$empty
 <p>
 Het blok <tt>$block</tt> is nog  niet aangemaakt.
 <p>
 !exit
!endif

!if $datetest<0
 <p>
 Deze pagina is nog niet publiek beschikbaar. Kom later maar eens terug ! 
 <p>
 !exit
!endif

!if $datetest>0
 <p>
 Deze pagina is verlopen, en is dus helaas niet meer beschikbaar !
 <p>
 !exit
!endif

<h3>$titb</h3>

!if $sheetdata=$empty
 <p>
 Er staan geen oefeneningen in deze pagina.
 <p>
 !exit
!endif
Hier staan de oefeningen van deze pagina (onder werkblad formaat). 
U kunt ze via "cut & paste" invoeren
!if $wims_user=supervisor
 in een werkblad naar keuze uit uw klas.
!else
 in een werkblad van een virtuele klas onder uw verantwoording.
!endif

<pre>
$sheetdata
</pre>

</div><div class=wimstail>
<p><hr><p>

<center>
!href cmd=reply&job=read Terug naar doc
</center> <p>

