
gestion des variables techniques et liaison tableur :
    - permettre de récupérer toutes les variables techniques (avec le nom d'étiquette)
    - permettre de modifier les variables techniques locale (à partir de l'étiquette)
    - seules les variables techniques créées seront modifiables par tableur.

questionnaire :
    - regarder l'interaction permettant de définir la valeur des variables techniques à l'aide d'un questionnaire.
    

Gestion de error=valuedisappear : permettre ou non de supprimer une valeur pour une variable technique (voir si cette valeur est en utilisation ou pas).
Si elle n'est pas en utilisation, accepter de supprimer sinon refuser.

importation des anciennes variables.
Il faut mettre un message d'alerte lorsqu'aucune nouvelle variable est créée et qu'il existe d'ancienne variable technique.