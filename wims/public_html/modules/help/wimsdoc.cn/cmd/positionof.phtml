!! +exec+ +var+ +string+
$lst_tit positionof$lst_titend$type_both
  $lst_alias position, positions<br>
  $(lst_syntax)positionof [$(emph)type$emphend] $emph s $emphend in $emph
  string$emphend</tt> <br>
  $lst_mean 返回对象 $emph s $emphend 在 $emph string$emphend 里的位置. <br>
  $emph type$emphend 可以是 <tt>char</tt>, <tt>word</tt>, <tt>item</tt> 或
  <tt>line</tt>. 如果被指定, 就在 $emph string$emphend 里寻找与 $emph s$emphend 完全相同的对象. <br>
  命令的输出是 $emph s$emphend 出现在 $emph string$emphend 里的位置表
  (作为字符, 词, 项或行, 由 $emph type$emphend 确定). <br>
  如果没有指定 $emph type$emphend, 则 $emph s$emphend 看作
  $emph string $emphend 的字串, 报告的位置是字符的位置.
  


