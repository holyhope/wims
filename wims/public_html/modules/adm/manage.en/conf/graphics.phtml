
!if $save!=$empty and $changed>0
 !href cmd=reply#check Click here
 to check the effect of the new setup.
 <p>
 !exit
!endif

<p><hr><p>
<a name=check></a>
!if new.png notin $cvtest
 <b>ERROR</b>! We didn't find a working ImageMagick on the system.
 Without this package, most WIMS images and animations will be broken. So we
 strongly recommend that you install ImageMagick before continuing.
 <p>
 !exit
!endif
!distribute char $useropts into us,up
!if $us!=$empty and $us>=0 and $us<=9
 !set wims_texsize=$[$wims_texbasesize-$us+1]
!endif

Check dynamic graphics (you may wish to do it with different browsers):
<p>
!set t=1
!set ins_align=middle
This formula
!set formula=\sqrt{x^2+y^2}=\sin(t^3)
!instex $$$$ $formula $$$$
 uses dynamic TeX insertion. It should appear correctly, unless <b>TeX</b> is not
correctly installed on your system.
You may change TeX font size step if they appear
too big or too small (read the 
!href cmd=help&help_subject=conf&help_conf=texbasesize help
 first!).
<p>
The following two pictures are generated by the internal dynamic drawing
program. One is still image, the other is animated. Can you see them
correctly? If not, try different graphics/animation formats.
<p><center>
<table border=0>
<tr><td>
!set insdraw_size=200,200
!insdraw fill 0,0,white\
transparent white\
xrange -1,1\
yrange -1,1\
disk 0,0,240,grey\
disk 0,0,200,red\
disk 0,0,160,cyan\
disk 0,0,120,green\
disk 0,0,80,blue\
disk 0,0,40,yellow
</td><td>&nbsp;&nbsp;&nbsp;</td><td>
!set insdraw_size=200,200
!set ins_anim_frames=15
!set ins_anim_delay =0.3
!set ins_anim_loop  =0
!insdraw fill 0,0,white\
transparent white\
v=1.3\
xrange -v,v\
yrange -v,v\
arrow -v,0,v,0,10,grey\
arrow 0,-v,0,v,10,grey\
linewidth 2\
levelcurve green,x^4+y^4+(cos(s*pi)*x-sin(s*pi)*y)*(sin(s*pi)*x+cos(s*pi)*y),0,-0.06,0.2,0.6,1
</td></tr>$table_end <p>

The following plot uses <b>gnuplot</b>. The curves should have different colors. If
this is not OK, try different graphics formats.
<p><center>
!set ins_border=2
!set insplot_set=size 0.6,0.6
!set insplot_colors= xffffff x000000 x404040 \
 x$color_1 x$color_2 x$color_3 x$color_4 x$color_5 x$color_6 x$color_7 x$color_8 x$color_9 x$color_10
!insplot [-5:5] [-1.4:1.4] sin(x),cos(x),-sin(x),-cos(x)
</center> <p>
The following plot uses <b>graphviz</b>. If it does not appear, then
!!!! no space after about ...
!exec graphviz about
 is not correctly installed
<p><center>
!set graphviz_dot=neato
!set graph=!exec graphviz digraph test {a->b [color=red] ; b-> c; a-> é ; c-> é}
!line 1 of $graph
</center>

<p>
The following ray-traced picture is not affected by the above formats. If it does not
appear, then 
!exec povray about
 is not correctly installed.
 !href cmd=help&help_subject=soft&help_conf=povray help

<p><center>
!set povray_width=200
!set povray_height=200
!set povsrc=!record 0 of conf/pov.src
!set rayout=!exec povray $povsrc
!line 1 of $rayout
</center> <p>

