

 <b>对象的一一对应.</b>
 (名称: <tt>correspond</tt>)
 <br>
用户会看到一个表, 两边列出相同个数的项, 但对应关系被打乱了.
用户应该点击这些项进行重排, 直至得到正确的对应关系.
 <p>
标准答案是两行的矩阵. 用分号 `;' 分隔两行, 第一行在左边,
第二行在右边.
 <p>
每一项可以包含文字、图形(HTML格式)以及数学公式(使用 <tt>\(...)</tt>).
 <p>
用户的回答栏最好使用 \embed{}, 它的输入大小的区段的形式为
<tt>VxH1xH2</tt>, 其中 V 是每项的竖直大小, H1, H2 分别是左右两列的水平大小.
 <p>
在默认情形, 不是全对的回答都得0分.
如果你在回答的可选 "option" 区段填入 <tt>split</tt>,
那么就按照答对的比例给分.
 <p>
 这是以DynAPI为基础的, 可能在有的浏览器里不能正常工作.

