<center><h1>$tit</h1></center>

!if $error!=$empty
 !read msg.phtml
 <p><hr> <p>
!endif

!if $erase!=$empty
 Le fichier <b>$erase</b> a été effacé.
 <p>
!endif

!if $quota_free<1
 Vous ne pouvez plus déposer de fichier dans ce document car son contenu est
 très proche,  ou a dépassé, la limite d'espace disque autorisé. Désolé.
!else
 !set wims_form_method=file
 !form reply
 <input type=hidden name=job2 value=deposit>
 Vous pouvez déposer un fichier dans ce document&nbsp;:
 <input type=file size=35 name=wims_deposit>
 <input type=submit value=OK>
 </form>
!endif

!if $flist!=$empty
 <p>Fichiers disponibles dans ce document&nbsp;:
 !for f in $flist
  !getfile doc/$f $f
  &nbsp;
 !next f
 <p>
 !form reply
 <input type=hidden name=job2 value=erase>
 Effacer un fichier&nbsp;:
 !formselect job2 list $flist
 <input type=submit value=OK>
 </form>
 <p>
 Ces fichiers sont accessibles dans le document à l'aide de la variable
 <tt>\filedir</tt>. Par exemple,
<p><center>
 <tt>&lt;a&nbsp;href=\filedir/$f1&gt;$f1&lt;/a&gt;</tt>
</center> <p>
 crée un lien vers le fichier <a href=$m_filedir/$f1>$f1</a>.
 Vous pouvez aussi écrire
<p><center>
 <tt>&lt;img src=\filedir/myfile.jpg&gt;</tt>
</center> <p>
pour inclure un fichier d'image <tt>myfile.jpg</tt> dans la page.
 <p>
 Les fichiers déposés dans ce document sont uniquement accessibles à ceux
 ayant le droit de lire le document.
!endif

<p>
Espace disque disponible au départ&nbsp;: $quota_total Meg.
Espace libre&nbsp;: $quota_free Meg.

<p><center>
!href cmd=reply&job=prop Propriétés du document
&nbsp;&nbsp;
!href cmd=reply&job=list Liste de blocs
&nbsp;&nbsp;
!href cmd=reply&job=read&block=main Bloc d'entrée

</center> <p>

