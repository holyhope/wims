
<b>画直线、多边形、点、线段、向量、矩形.</b>
(名称: <tt>javacurve</tt>)
<br>
这种回答类型允许用户在一个图形上画直线、多边形、线段、向量、矩形
或点.

<p>
标准答案用矩阵形式给出 (行分隔符是分号 `;').
第一行是图形的 URL(所在地址).
第二行是对用户图形的测试条件, 见下表:
<ul>
<li><tt>points,x1,y1,x2,y2,...</tt> 在 (x1,y1), (x2,y2), .... 的点.
这是有固定宽度的``厚''点.</li>
<li><tt>line,x1,y1,x2,y2</tt> (x1,y1) 与 (x2,y2) 的连线.</li>
<li><tt>sline,x1,y1,x2,y2</tt> 从 (x1,y1) 到 (x2,y2) 的半直线.</li>
<li><tt>polygon,x1,y1,x2,y2,...</tt> 顶点在 (x1,y1), (x2,y2), ... 的多边形.</li>
<li><tt>rectangle,x1,y1,x2,y2</tt> 从 (x1,y1) 到 (x2,y2) 的矩形.</li>
<li><tt>rectangle,x1,y1,x2,y2,x3,y3,x4,y4</tt> 一个矩形, 它包含在从 (x1,y1) 到 (x2,y2) 的矩形内、
且包含从 (x3,y3) 到 (x4,y4) 的矩形.</li>
<li><tt>segment,x1,y1,x2,y2</tt> 线段 (x1,y1), (x2,y2).</li>
<li><tt>vector,x1,y1,x2,y2</tt> 从 (x1,y1) 到 (x2,y2) 的向量.</li>
<li><tt>circle,x1,y1,r</tt> 中心在 (x1,y1), 半径为 r 的圆周.</li>
</ul>
</p><p>
建议把输入栏嵌入陈述中. 它不能与其它输入栏共存.

</p>
一般说来, 变量
<tt>\reply</tt><em>n</em> 被用于 \feedback 记录了回答时的点击 (x1,...),
其中 <em>n</em> 是输入栏的号码.
不过以下情形是例外:
<ul><li>
在 <tt>points</tt> 的情形,
第一行是点击坐标(以点素pixel为单位)的表,
第二行是 <tt>n1,n2,n3</tt>, 其中
<tt>n1</tt> 正确点的个数, <tt>n2</tt> 是散失点的个数,
<tt>n3</tt> 是错误点的个数. 第三行是找对的点的个数表.
</li><li>
在 <tt>circle</tt> 的情形, 第一行是点击坐标以及半径的表,
第二行是 <tt>n1,n2</tt>, 
当中心正确时 <tt>n1</tt> 取 1, 否则取 0,
当半径正确时 <tt>n2</tt> 取 1, 否则取 0.
</li>
<li>
在 <tt>sline</tt> 的情形, 第一行是点击坐标表,
第二行是 <tt>n1,n2,n3</tt>, 
当起点正确时 <tt>n1</tt> 取 1, 否则取 0,
当斜率正确时 <tt>n2</tt> 取 1, 否则取 0,
当指向正确时 <tt>n3</tt> 取 1, 否则取 0.
</li>
</ul>
