!!<html><head>
!!$wims_html_header
!!</head><body $wims_htmlbody>
!!!headmenu

!header1

!if $sheet_docpage!=$empty
 !changeto docpage.phtml
!endif
!read adm/title.phtml 1\
$wims_classname, $wims_institutionname\
WWerkblad #$sh<br>$sheet_title

!!<h2>$wims_classname, $wims_institutionname</h2>
!!<hr><h3 class="wims_sheet_title">WWerkblad #$sh<br>$sheet_title</h3>
<div class="wims_sheet_desc">$sheet_desc</div><p>

!if $sheet_expiration!=$empty and $sheet_active=1
 !set expday=!char 7,8 of $sheet_expiration
 !set expmon=!char 5,6 of $sheet_expiration
 !set expyear=!char 1 to 4 of $sheet_expiration
 !set months=Januari,Februari,Maart,April,Mei,Juni,Juli,Augustus,September,Oktober,November,December
 !set expmon=!item $expmon of $months
 Je kunt aan dit werkblad werken <b>tot</b> $expday $expmon $expyear.
!endif

!if $sheet_active=2
 <b><font color=red>Waarschuwing</font></b>
 Dit werkblad is <b>verlopen</b>.<br>
 Je kunt er nog wel met werken, maar de cijfers worden niet langer bewaard.
 <p>
!endif

!if $n=0
 !if $sheet_active=$empty
 Je hebt -nog- niet de rechten met dit werkblad te werken ; vraag na bij je docent.
 !else
 Dit werkblad is leeg. Er is dus voorlopig voor jou hier niks te doen ;-)<br> 
 Mogelijk een foutje van je docent ?
 !endif
!else
De opdrachten in dit werkblad: (cijfer registratie is
 !set scorestat=!getscorestatus sheet=$sh
 !if $scorestat=yes
  !if $wims_scorereg!=suspend
   open, maar je kunt het
   !href cmd=resume&job=suspend opschorten
.)
!set wims_menu_items=!append line scoreclose2,1,cmd=resume&job=suspend\
to $wims_menu_items
  !else
   !set scorestat=no
   door jezelf opgeschort.
   !href cmd=resume&job=reopen reactiveren
.)
!set wims_menu_items=!append line scorereopen,1,cmd=resume&job=reopen\
to $wims_menu_items
  !endif
 !else
  geloten voor jou.)
 !endif
 <p><ul>
 !for i=1 to $n
  !set ex=!record $i of wimshome/log/classes/$wims_class/sheets/.sheet$sh
  !distribute lines $ex into di,pa,require,we,ti,de
  !set got=!word $i of $scores
  !set mean=!word $i of $means
  !set dep=!row $i of $deps
  !set dep=!trim $dep
  <li><p>$i.
  !if $dep=$empty or $scorestat!=yes
   !href module=$di&cmd=new&worksheet=$sh&$pa $ti
, $de
   !if $require>0
    <small>$[$got] uit $[$require] punten behaald, een gemiddelde score van
    $[$mean]/10.</small>
   !endif
  !else
   <font color=red><b>$ti</b></font>, $de 
   !if $require>0
    <small>($[$require] benodigde punten.)</small>
   !endif
  !endif
  !if $dep!=$empty
   <small>je moet dit verbeteren; <br>
   !if , isin $dep
    je cijfers voor deze oefeningen $dep
   !else
    je cijfer voor deze oefening $dep
   !endif
   </small>
  !endif
 !next i
 </ul>
!endif

:contentend
!if $wims_user=supervisor
<p>
!!!href module=adm/class/sheet&sheet=$sh Werkblad aanmaak pagina
!! (voor docenten ).&nbsp;
!set wims_menu_items=!append line sheetmanagement,1,module=adm/class/sheet&sheet=$sh\
to $wims_menu_items
!endif
!if $n>0
!set wims_menu_items=!append line printable,1,cmd=reply&job=print\
to $wims_menu_items
!!!href cmd=reply&job=print Printbare versie.
!if $exolog!=$empty
!! !href module=adm/class/exolog&job=llist&logsheet=$sh Registreer de details van deze oefening
!!.
  !set wims_menu_items=!append line exolog,1, module=adm/class/exolog&job=llist&logsheet=$sh \
to $wims_menu_items 
 !endif
!endif
!tail

