
!read search.phtml
!if $clcnt+$sccnt<1 and $keyword!=$empty
 Non ci sono corsi corrispondenti alla ricerca. <p>
!endif

!if $wims_user!=$empty
 !read direct.phtml
 !if $wims_user!=supervisor and $supervisable!=yes
  !exit
 !endif
 <p>
!else
 I corsi interattivi virtuali permettono ai docenti di organizzare le 
 risorse WIMS disponibili (cui eventualmente aggiungere esercizi
 loro creazione) per metterle a disposizione dei loro studenti, e per
 seguire i progressi degli studenti.
 <p>
 Se non lo avete ancora fatto, potete
 !href module=adm/class/regclass creare il vostro corso interattivo
 (dopo aver consultato la 
 !href target=wims_help module=help/main&chapter=3 documentazione
), o aver visionato gli
 !href module=$module&type=example esempi disponibili
. <p>
!endif

!if $overflow=yes
 !exit
!endif

!if $clcnt+$sccnt>0
 !if $wims_user=empty
  Altrimenti selezionate il vostro corso nella nella tabella che segue 
  (cliccando sul nome si accede alle pagine di gestione).
 !endif
 !if $sccnt>0
  <p>
  $table_header
  <caption>Sedi e corsi coordinati</caption>
  $table_hdtr<th>Nome</th><th>Gestore</th>
  <th>-</th>
  </tr>
  !for i=1 to $sccnt
   !set l=!line $i of $sclasses
   !distribute items $l into code,expire,in,cl,la,ty,lv,pswd
   !set supv=!item 9 to -1 of $l
   !default supv=!defof class_supervisor in wimshome/log/classes/$code/.def
   
   $table_tr
   <td align=center>
   !href module=$module&type=authparticipant&class=$code $in
   </td>
   </td><td align=center>$supv</td>
   <td align=center>
   !href module=adm/class/reguser&class=$code Registrazione
&nbsp;
   !href module=$module&type=authsupervisor&class=$code Gestione
   </td>
  !next i  
  $table_end
 !endif
 !if $clcnt>0
  <p>
  $table_header
  !if $superclass=$empty
   <caption>Corsi autonomi</caption>
  $table_hdtr<th>Istituto<th>Nome del corso<th>Docente
  !else
   !if $splevel>1
    !set inst=!defof class_description in wimshome/log/classes/$superclass2/.def
  !else
   !set inst=!defof class_institution in wimshome/log/classes/$superclass/.def
   !endif
   <caption>$Name_levels in $inst 
   !if $splevel>1
    !href cmd=reply&type=$type&superclass2=$sparent Up
   !endif
   </caption>
   $table_hdtr<th>$Name_level<th>Docente
   !if $superclass!=$empty and $stype=4
    <th>More
   !endif
  !endif
 !for i=1 to $clcnt
  !set l=!line $i of $classes
   !distribute items $l into code,expire,in,cl,la,ty,lv,pswd
   !set supv=!item 9 to -1 of $l
   !default supv=!defof class_supervisor in wimshome/log/classes/$code/.def
   
   $table_tr
   !if $superclass=$empty
    <td align=center>$in</td>
   !endif
   <td align=center>
   !href module=$module&type=authsupervisor&class=$code $cl
   </td><td align=center>$supv</td>
   !if $superclass!=$empty and $stype=4
    <td>
    !if $ty!=1
     !href cmd=reply&type=$type&superclass2=$code subclasses
    !else
    -
    !endif
   !endif
 !next i
 !endif
 $table_end
!else
 !if $class_type=$empty and $superclass=$empty
 (In questo server non sono presenti corsi in lingua italiana.)
 !else
  (Non ci sono corsi.)
 !endif
!endif
<p>

