!if $wims_read_parm=help
!if $lang=cn
把参数不加赋值地写进前置格式.

<p><b>例:</b>
<pre>\text{texte=
for a in \B
   a = a + 1
endfor
}
\statement{ \special{expandlines \texte}}
</pre>
 !exit
!endif

!translate $    $ to $\
$ in <pre>$wims_read_parm</pre>
