!! +exec+ +var+ +string+
$lst_tit detag$lst_titend$type_both<br>
  $(lst_syntax)detag $emph string$emphend</tt> <br>
  $lst_mean 除去 $emph string$emphend 里的html标记(html tag). 
  本命令相当原始, 使用时要小心. 在某些情况下用于增强安全性(应用于用户提供的串).

