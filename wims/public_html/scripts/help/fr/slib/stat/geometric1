slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire suivant la loi géométrique sur N*

slib_out=Simulation de <tt>m</tt> réalisations d'une variable aléatoire suivant la loi géométrique de paramètre <tt>p</tt> sur N*.\
Si <tt>m</tt> est un vecteur à deux éléments entre crochets <tt>[r,s]</tt>, \
renvoie une matrice à <tt>r</tt> lignes et <tt>s</tt> colonnes.
slib_comment=Si X suit cette loi P(X=k)=p(1-p)^(k-1) pour tout entier strictement positif k.