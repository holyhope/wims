
<p><h3><center>Añadir la conexión a un servidor</center></h3> <p>

!read connect/remote.phtml

!if $error!=$empty
 !exit
!endif

!form reply
<input type=hidden name=job2 value=add2>
Para conectarse a una clase en el servidor de más arriba, por favor especifique:
<p>
Identificador de la clase remota:
<input size=20 name=addclass>
<br><small>Si la clase remota está vacía, el servidor creará
una nueva clase en el servidor remoto, duplicando las propiedades de esta.
</small>
<p>
Actualizar automáticamente la clase remota con los cambios efectuados:
!formselect connupd1 list 0,1 prompt no,sí
<br>
Permitir que la clase remota modifique esta:
!formselect connupd2 list 0,1 prompt no,sí

<p><center><input type=submit value=OK></center>
</form>

!reset addclass

