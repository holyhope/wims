!set wims_module_log=error: $error

!if not_supervisor=$error
 Lo sentimos, pero sólo puede acceder a esta página el profesor registrado de
 la clase.
 !exit
!endif

!if bad_class=$error
 Extraño, pero ¡¡su clase no es válida!!
 !exit
!endif

!if too_many_users=$error
 Su clase tiene demasiados participantes ($ucnt).
 Las puntuaciones se pueden introducir manualmente
 sólo para clases con un máximo de $maxuser participantes. 
 Lo sentimos.
 !exit
!endif

!msg $error

