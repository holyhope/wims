
!if $vote_option=anonymous
 这是匿名投票.
!else
 这是记名投票.
!endif
!if $vote_status=2
 已经关闭.
!endif

!if $wims_user=supervisor
 ($vcnt 票)
 !href cmd=reply&job=edit 修改
<p>
!endif

!if $vote_status=2
 !changeto $votedir/$vote.def
!endif

!if $(user_vote_$Vote)!=$empty
 !if $vote_option=anonymous
  你已经投过票了. <p>
 !else
  你已经表示过你的选择, 不过你还可以改变主意.
  <p>
  !goto form
 !endif
!else
 你的选择: <p>
 :form
 !form reply
 <input type=hidden name=job value=投票>
 !set m_formed=yes
!endif

!read $votedir/$vote.def

!if $m_formed=yes
 <p><center>
 <input type=submit value="发送">
 </center></form>
!endif

!if $wims_user=supervisor and $vote_option=nominative
 <p>  详细记录.
 !for i=1 to $parmcnt
  !for c=1 to $(choices$i)
   !if /supervisor/ notin $c/
    <p>
    !item $c of $(chlist$i)
 <br> <tt>
    !for u in $(votes_$(i)_$c)
     !set U=!translate internal / to , in $u
     !readproc adm/class/userdef classes,$U
     !defof user_lastname, user_firstname in $userdef
     ($u)
     <br>
    !endif
   !next u
   </tt>
  !next c
 !next i
!endif

