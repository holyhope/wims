slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire suivant la loi beta

slib_out=Simulation de <tt>m</tt> réalisations d'une variable aléatoire suivant la loi \
beta de paramètres <tt>a</tt> et <tt>b</tt> (strictement positifs).\
Si <tt>m</tt> est un vecteur à deux éléments entre crochets <tt>[r,s]</tt>, renvoie une matrice à <tt>r</tt> lignes\
et  <tt>s</tt> colonnes.
slib_comment=La densité en x est proportionnelle à x^(a-1)(1-x)^(b-1) sur l'intervalle (0,1).
