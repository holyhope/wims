!! +exec+ +string+
$lst_tit eval$lst_titend$type_var
  $lst_alias evalue
  $(lst_syntax)eval $emph expr$emphend</tt>
  $lst_mean 返回表达式 $emph expr$emphend 的数值, 
  而且是实数(对于目前的wims版本). 内部计算是以双精度进行的,
  但打印精度由变量 $emph wims_print_precision $emphend 确定(其默认值是 8). <br>
  如果在表达式 $emph expr$emphend 内出错, 则返回 0.
  (我知道这不好; 但目前我还没有时间搞定.)

