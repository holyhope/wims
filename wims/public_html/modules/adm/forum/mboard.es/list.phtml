
!read desc.phtml
<p>
!if $tot<=0
 !if $mlist!=.newlist
  Ningún mensaje accesible corresponde a su petición. Lo sentimos.
 !else
  Este foro está vacío todavía.
 !endif
!else
 !if $mlist=.newlist
  Lista de los últimos mensajes, los más
 !else
  Lista de mensajes del mes de
  !item $lmon of $monthnames
  $lyear, los más
 !endif
 !item $l_order of recientes primero,antiguos primero
. <small>
 !href cmd=reply&l_order=$[$l_order%2+1] [Orden inverso]
 !if $send_right>0
  &nbsp;
  !href cmd=new&forum=$forum&job=compose [Nuevo mensaje]
 !endif
</small>
 <ul>
 !set lh=<li>
 !if $l_order=1
  !distribute item $tot,1,-1 into start,end,step
 !else
  !distribute item 1,$tot,1 into start,end,step
 !endif
 !for i=$start to $end step $step
  !if $mlist=.newlist
   !set l_=!line $i of $mnew
   !distribute word $l_ into a_,b_
   !set l_=!record $b_ of $forumdir/$a_/.msglist
  !else
   !set l_=!record $i of $forumdir/$mlist/.msglist
  !endif
  !read listone.phtml $l_
 !next i
 </ul>
!endif

<p>
!if $mlist!=.newlist
 !href cmd=reply&job=list&mlist=.newlist Últimos mensajes.
 &nbsp;
!endif

!read listarch.phtml

