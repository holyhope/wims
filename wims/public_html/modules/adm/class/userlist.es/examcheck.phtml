
<center>$classname
<p>
<h1>Detalles de los exámenes
par $user_lastname, $user_firstname
</font>
</h1></center>

!if _check isin $session
 !set error=bad_op
 !goto msg
!endif

!if $error!=$empty
 :msg
 <p>
 !read msg.phtml
 <p>
!else
 !if $checksessions=$empty
  Este participante no ha realizado aún ningún examen.
  <p>
 !else
  Sesiones de examen efectuadas por este participante (pulse sobre un
  ejercicio para ver su contenido):
  <p>$table_header
  $table_hdtr<th>Sesión</th><th>Examen</th><th>Puntuación<small>/10</small></th>
  <th>Ejercicio</th><th>Comienzo</th></tr>
  !for ses in $checksessions
   !for x in $(exam$ses)
    $table_tr<td><small>$ses</small></td>
    <td align=center>$x</td>
    <td align=center>$(score$(ses)$x)</td><td>
    !for i in $(exo$(ses)$x)
     &nbsp;
     !href target=wims_check module=$module&cmd=new&job=examcheck&checkuser=$checkuser&checksession=$ses&checkexo=$x.$i&checkstep=1 $i
    !next i
   !next x
   </td><td><small>$(start$(ses)$x)</small></td>
  !next t
  $table_end <p>
 !endif
!endif

<center>
!href cmd=reply&job=getuser&getuser=$checkuser Otras puntuaciones
&nbsp;
!href cmd=resume Lista de los participantes

