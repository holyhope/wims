
<h1 class="wims_title">$tit</h1>

!if $error!=$empty
 !read msg.phtml
 <p><hr> <p>
!endif

!if $edtype=file
 !set wims_form_method=file
!else
 !set wims_form_method=post
!endif
!form reply
<input type=hidden name=doc value=$doc>
<input type=hidden name=block value=$block>

!if $titb=$empty
 Creation
!else
 Modification
!endif
of the block <tt>$block</tt>.
!if $edtype=file
 !href cmd=reply&edtype=textarea [Online composition]
!else
 !href cmd=reply&edtype=file [Insert a file]
!endif
<br>

!set titb=!reinput $titb
Title: <input size=50 name=Titb value="$titb"> <br>
!if $edtype=file
 Insert a file into the content : <input type=file size=35 name=wims_deposit>
 !if $titb!=$empty
  <br>This file will
  !formselect addtype list replace,append,prepend prompt\
	replace, append to, prepend to
  the existing content.
 !endif
 <br>
!else
 !mexec scripts/textarea.sh
!endif
<center>
<input type=submit name=preview value="Preview">
<input type=submit name=rEg value="Register">
<input type=submit name=abandon value="Cancel">
</center> <br>
!! Key words: <input size=50 name=Keyw value="$keyw"> <br>
!if $block!=main
 Previous block: <input size=12 name=Prev value="$prev">
 next: <input size=12 name=Next value="$next">
 parent: <input size=12 name=Upbl value="$upbl">
 <br>
!endif
Validity dates <small>aaaammjj</small> : from
<input size=8 name=Dat1 value="$dat1"> to
<input size=8 name=Dat2 value="$dat2">
</form>
!if $edtype=file
 <p><b>Remark</b>. Accepted file formats:
 WIMS document source, html file or LaTeX source. Partial
 source files can be submitted, provided that they are properly cut off 
 (that is, not cut at the middle of an html tag, a math formula, or the like).
!endif
<p><hr> <p>


