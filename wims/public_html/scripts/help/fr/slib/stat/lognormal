slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire de loi lognormale

slib_out=Simulation de <span class="tt">N</span> réalisations d'une variable aléatoire suivant la loi lognormale \
de paramètres <span class="tt">mu</span> et <span class="tt">sigma</span>.\
Si une variable aléatoire suit cette loi, son logarithme\
suit la loi normale d'espérance <span class="tt">mu</span> et d'écart-type <span class="tt">sigma</span>.

slib_comment=Si <span class="tt">m</span> est un vecteur à deux éléments entre crochets <span class="tt">[r,s]</span>, \
renvoie une matrice à <span class="tt">r</span> lignes et <span class="tt">s</span> colonnes.

