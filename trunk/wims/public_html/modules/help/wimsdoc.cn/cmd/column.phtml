!! +var+ +exec+ +string+
tit:column
type:$type_both
alias:columns
syntax:!column `numlist' of `string'
mean:输出按照 `numlist' 在矩阵 `string' 里选出的列(用逗号分隔).\
  矩阵的行可以用换行号或分号 ``;'' 分隔. <p>\
  如果只选一列, 则输出用逗号分隔, 否则按照矩阵 `string' 的分隔方式.
