# @configure_input@

wims_home=../../..
cc=@CC@
cflags=@CFLAGS@ -Wall
defines=@DEFINES@
lopt=-lm

LDFLAGS =

all: toascii

clean:
	rm -f *.o *.core *.stackdump toascii

toascii: toascii.c
	$(cc) $(cflags) $(LDFLAGS) -o toascii toascii.c $(lopt)
