!! +exec+
$lst_tit mexec$lst_titend
  本命令只能在网站的$emph 可信任$emphend 模块内执行.
  $type_both
  $(lst_syntax)mexec $emph command parms$emphend</tt>
  $lst_mean 执行模块私有的外部命令 $emph command$emphend,
  带有可选参数 $emph parms$emphend. $emph command$emphend 
  必须是模块主目录里的有效程序名. 但命令在服务器的基准目录里执行. <br>
  当用于变量命令时, 返回被调用程序的输出.
