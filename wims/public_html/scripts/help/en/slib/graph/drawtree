slib_lang_exists_tmp=yes

slib_title=Tree draw
slib_parms=3\
,representation of the tree G in [] \
, text :  [[text for edges],[text for vertices]]\
raw, option words : <tt>middle</tt>, <tt>point</tt>, <tt>raw</tt>, <tt>url</tt>, <tt>html</tt>

slib_out=drawing of a given tree representation of the tree, matrix of the graph,matrix of the positions of edges ??,\
matrix of the positions of middle of vertices,\
option : middle, point
slib_comment=the options mean that a circle is made\
at the vertex or on the middle of the edges. \
The tree is represented in the following way : G is inclosed in brackets. \
Inside each line, first item is a father and the other items its sons.\
No verification is made.\
The first item of the output is: by default, the flydraw code; \
the address of the generated image if the option word is <tt>url</tt> ; \
the html code for inserting image is already inserted for the option <tt>html</tt>.
