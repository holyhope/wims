
Le seguenti risorse contenute nel file di backup del corso sono state ripristinate: $restored.

!if $user_bad!=$empty
 <p>
 Il ripristino degli accountdegli studenti (e dei registri delle attività)
 non è stato possibile per mancanza di spazio.
!endif

!if $exp_bad!=$empty
 !distribute items méconnaissable, trop tôt, trop tard into ebad,eearly,elate
 <p>
 La data di termine del corso è stata modificata dopo il ripristino, poiché la
 la data impostata nel file di backup è$(e$exp_bad).
!endif

