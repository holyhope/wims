
!read adm/title.phtml 3\
\
增加服务器连接


!read connect/remote.phtml

!if $error!=$empty
 !exit
!endif

!form reply
<input type=hidden name=job2 value=add2>
为连接到上述服务器的班级, 请提供:
<p>
远程班级的身份:
<input size=20 name=addclass>
<br><small>如果远程班级是空的, 服务器将在远程服务器上创建一个新班级,
复制本班级的属性.
</small>
<p>
根据本班级的修改自动完成远程班级的自动更新:
!formselect connupd1 list 0,1 prompt 不,是
<br>
允许远程班级修改这个:
!formselect connupd2 list 0,1 prompt 不,是

<p><center><input type=submit value=提交></center>
</form>

!reset addclass

