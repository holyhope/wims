The files in this directory are used both by modind and by WIMS search
engine.

Note: modind creates the lists in ../site/, such files are used by the
search engine (~/public_html/modules/home/search.proc)

which file is used by which program
which files are automatically generated 

indignore: words ignored by modind

modind: word,wgrp,suffix,indignore
search.proc: words,suffix


domain.fr vs domain.fr
