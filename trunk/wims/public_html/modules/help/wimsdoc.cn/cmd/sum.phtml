!! +var+ +exec+ +string+
tit:sum
type:$type_both
alias:!add
syntax:!sum `fn' for `v'=`v1' to `v2' [step `st']\
$ !sum `fn' for `v' in `v1,v2,v3,...'
mean:把变量 `v' 用 `v1' 与 `v2' 之间的值逐次替换后, 计算函数 `fn(v)' 值之和. <p>\
  在第二种情形, 用表 `v1,v2,...' 里的值逐次替换.</p>
