
Alle logische beweringen zijn <em>string</em> gebaseerd: <tt>string1 rel string2</tt>.
Meerdere beweringen kunnen worden gekoppeld door de sleutelwoorden <tt>and</tt> en<tt>or</tt>. 
Gebruik haakjes voor complexe constructies.<p>
:Beschikbare types <tt>rel</tt>:
:string1 relatie $string2,conditie
:string1 = string2</tt> of <tt>string1==string2
is waar als beide strings identiek zijn.
:string1 != string2</tt> or <tt>string1&lt;&gt;string2
is waar als beide strings niet gelijk zijn.
:string1 &lt; string2
is waar als de numerieke evaluatie van string1 kleiner is dan string2<br>de nauwkeurigheid wordt bepaald door de variabele <tt>wims_compare_precision</tt>
:string1 &lt;= string2
is waar als de numeriek evaluatie van string1 kleiner of gelijk is aan string2
:string1 &gt; string2
is waar als de numerieke evaluatie van string1 groter is dan string2
:string1 &gt;= string2
is waar als de numeriek evalulate van <tt>string1</tt> groter of gelijk is aan <tt>string2</tt>.
:string1 isin string2
is waar als <tt>string1</tt> een sub_string is van <tt>string2</tt>.
:string1 notin string2
is waar als <tt>string1</tt> geen sub_string is van <tt>string2</tt>.
:string1 iswordof string2
is waar als <tt>string1</tt> een woord is uit<tt>string2</tt>.
:string1 notwordof string2
is waar als <tt>string1</tt> geen woord is uit <tt>string2</tt>.
:string1 isvarof string2
is waar als <tt>string1</tt> een (wiskundige) variabele is van de formule <tt>string2</tt>.
:string1 notvarof string2
is waar als <tt>string1</tt> geen (wiskundige) variabele is van de formule <tt>string2</tt>.
:string1 isvariableof string2
is waar als <tt>string1</tt> een (wiskundige) variabele is van de formule <tt>string2</tt>.
:string1 notvariableof string2
is waar als <tt>string1</tt> geen (wiskundige) variabele is van de formule <tt>string2</tt>.
:string1 isitemof string2
is waar als <tt>string1</tt> een item is uit lijst <tt>string2</tt>.
:string1 notitemof string2
is waar als <tt>string1</tt> geen item is uit lijst <tt>string2</tt>.
:string1 islineof string2
is waar als <tt>string1</tt> een regel is uit lijst <tt>string2</tt>.
:string1 notlineof string2
is waar als <tt>string1</tt> geen regel is uit lijst <tt>string2</tt>.
:string1 issamecase string2
is waar als <tt>string1</tt> en <tt>string2</tt> dezelfde tekst zijn, onafhankelijk van spaties maar wel hoofdletter gevoelig.
:string1 notsamecase string2
is waar als <tt>string1</tt> en <tt>string2</tt> niet dezelfde tekst zijn, onafhankelijk van spaties maar wel hoofdletter gevoelig.
:string1 issametext string2
is waar als <tt>string1</tt> en <tt>string2</tt> dezelfde tekst zijn, onafhankelijk van accenten,meerdere spaties of hoofdletters.
:string1 notsametext string2
is waar als <tt>string1</tt> en <tt>string2</tt> niet dezelfde tekst zijn, onafhankelijk van accenten,meerdere spaties of hoofdletters.

