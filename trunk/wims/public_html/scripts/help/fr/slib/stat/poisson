slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire suivant la loi de Poisson.

slib_out=Simulation de <span class="tt">m</span> réalisations d'une variable aléatoire suivant la loi de Poisson\
de paramètre <span class="tt">lambda</span> (nombre positif).

slib_comment=Si X suit une telle loi, pour un entier positif i,  P(X=i)=exp(-lambda)*(lambda^i)/i!