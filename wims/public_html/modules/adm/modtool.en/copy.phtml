!if $error!=$empty
 !goto end
!endif

!if $originheader!=$empty and $job=move
 <b>$wims_name_warning</b>! Renaming the module will break all the worksheets
 referencing it. Make sure that this is not the case, or just
 !href cmd=reply&jobreq=copy duplicate it
.
 <p>
!endif

!if $target=$empty
 !form reply
 <input type=hidden name=jobreq value="$job">
 !distribute item Copy,Rename,Duplicate,Rename into P_copy,P_move,L_copy,L_move
 !if $originheader!=$empty
  $(L_$job) the module <em>$otit</em> (<tt>$original2</tt>) to <p>
 !else
  $(P_$job) the public module <em>$otit</em> (<tt>$original2</tt>) to the
  development space, under the address <p>
 !endif
 !read scripts/addressform.phtml
 </form>
 !if $originheader!=$empty and $o1 isitemof $zonelist,local
  <p>
  <b>$wims_name_warning</b>. Remaning or duplicating a public-address module
  may lead to duplication of its publication. Please be very careful!
  <p>
  If you want to make tests on the module, put it into the test zone.
 !endif
 !if $originheader!=$empty and ~ notin $original2
  <p>
  <b>$wims_name_warning</b>. If you have already published this module, please change
  its address exactly according to its public address. Otherwise its 
  publication will be duplicated.
 !endif
!endif




:end
!if $original2!=$empty and $originheader=$empty
 !default otit=!module title $original2
 <p>
 !href module=$original2 $name_backto
 <p>
!endif


