!! +var+ +exec+ +string+
$lst_tit lookup$lst_titend$type_both
  $(lst_syntax)lookup $emph defname$emphend in $emph fname$emphend</tt>
  $lst_mean 返回文件 $emph fname$emphend 内对词 $emph defname$emphend 的定义,
  文件 $emph fname$emphend 必须是词典文件, 语法按程序 <tt>translator</tt>
  的规定(不必排序).
  <p>
  $emph fname$emphend 或者位于模块目录里(此时不必指明),
  或者位于目录 bases/.

