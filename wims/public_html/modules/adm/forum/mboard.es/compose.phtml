
!if +$error iswordof +no_send_right
 !exit
!endif

!if $c_prec!=$empty
 El nuevo sigue a
 !href cmd=reply&job=read <b>$m_subject</b>
 por
 !if $m_email!=$empty
  <i><a href="mailto:$m_email">$m_from</a></i>
 !else
  <i>$m_from</i>
 !endif
 <p>
!endif

!form reply
 Nombre:
 !if $forumuser=$empty
  <input size=15 name=c_sender value="$c_sender">
 !else
  <tt>$c_sender</tt>;
 !endif
 correo electrónico: <input size=30 name=c_smail value="$c_smail"> <br>
 !if $send_right<2
  <b>Atención.</b> Su mensaje no será aceptado, a menos que dé una 
  dirección de correo electrónico QUE FUNCIONE. <br>
 !endif
 Tema:
 <input size=55 name=c_subject value="$c_subject"> <br>
 Cuerpo del mensaje:<br>
 !set msgsrc1=<textarea cols=70 rows=18 name=wims_deposit wrap>
 !set msgsrc2=</textarea>
 !if $c_quote>0 and $c_prec!=$empty
  !mexec scripts/quote.sh
 !endif
 !if $c_quote=0
  !mexec scripts/msgsrc.sh
 !endif
 !if $c_quote<0
  $msgsrc1$msgsrc2
 !endif
 !set c_quote=0
 $tag_escaper</table>
 <p><center>
 <input type=submit name=c_preview value="Vista previa">
 &nbsp;&nbsp;
 <input type=submit name=c_send value="Enviar">
 &nbsp;&nbsp;
 <input type=submit name=c_list value="Cancelar">
 </center> <p>
</form> <p>
!href cmd=help Ayuda
 sobre cómo escribir fórmulas matemáticas en sus mensajes.

!if $c_prec!=$empty
 <p><hr> <p>
 !href cmd=reply&job=read Mensaje original
  by <i>$m_from</i>:<p>
 !read $forumdir/$c_prec html
 $tag_escaper</table></form><font size=+0>
 <p><hr> <p>
!endif

