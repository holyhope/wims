!if $activetest>0
 Sorry, this exam is activated, hence non-modifiable. No exercise in it can 
 be changed.
 !exit
!endif

!form reply
<input type=hidden name=job value=exo>
<input type=hidden name=exo value=$exo>
<p>$wims_name_Exercise $exo : $name_weight=<input size=5 name=eweight value="$eweight">,
$wims_name_title:<input size=20 name=ename value="$ename">
<p>
This exercise can be (randomly) taken from a list of existing exercises in
worksheets. Please mark the worksheet exercises in the following list, which
you want to be candidate of this exam exercise.
<p>
show only exercices of sheet 
 <select name="ltsheets" onchange="submit()" value="$ltsheets">
  !if $ltsheets=$asheets
   <option value="$asheets" selected>$name_All</option>
  !else
   <option value="$asheets">$name_All</option>  
  !endif
  !for k in $asheets
   !if $ltsheets=$k
    <option value="$k" selected>
   !else
    <option value="$k">   
   !endif
    !let t=!record $k of  wimshome/log/classes/$wims_class/sheets/.sheets
    $k :
    !line 3 of $t
   </option>
  !next k
 </select>
<p>
!for i in $ltsheets
 !set t=!recordcnt wimshome/log/classes/$wims_class/sheets/.sheet$i
 !for j=1 to $t
  !set r=!record $j of wimshome/log/classes/$wims_class/sheets/.sheet$i
  !distribute line $r into r1,r2,r3,r4,r5,r6
  <input type=checkbox name=echoose value=$i.$j
  !if $i.$j isitemof $echoose
   checked
  !endif
  > $i.$j <b>$r5</b> <small>$r6</small> <br>
 !next j
!next i
<p>
$name_dependency: <input size=40 name=edep value="$edep"> <br>
$name_Options:
!formcheckbox eopt list $optionlist
<br>
<input type=submit value="$wims_name_tosave">
</form>
<p>
!href cmd=reply $name_mainpage
. <p><small>
<b>$wims_name_help</b>. 
Score dependencies means that a participant must get
  required scores in other exercises of the sheet before he/she can do
  this one. For example, 
  <font color=blue><tt>1:50,2:30,3+4+5:60</tt></font>
  means that the
  participant must get 50% success on exercise 1, 30% success on exercise 2,
  and an average of 60% success on exercises 3,4,5.
  <p>
  Options: <ul>
  <li><em>autogen</em> means that the exercise is automatically generated
  even if the user doesn't click on it. So that he can consult the registered
  exercise even when the session is closed. Not recommended except for
  manually scored exercises.
  </ul>
</small>


