
!read adm/title.phtml 3\
\
Voeg een server verbinding toe

!read connect/remote.phtml

!if $error!=$empty
 !exit
!endif

!form reply
<input type=hidden name=job2 value=add2>
Om een verbinding te maken met een klas van bovenstaande server, specificeer:
<p>
Naam van de klas op de andere server:
<input size=20 name=addclass>
<br><small>Wanneer deze klas leeg is, zal WIMS een nieuwe klas proberen aan te maken
op de andere server.(die dan een kopie is van deze klas) 
</small>
<p>
Automatische update van de klas op andere server ?
!formselect connupd1 list 0,1 prompt nee,ja
<br>
Sta de andere server toe deze klas -op deze server dus- te updaten
!formselect connupd2 list 0,1 prompt nee,ja

<p><center><input type=submit value=OK></center>
</form>

!reset addclass

