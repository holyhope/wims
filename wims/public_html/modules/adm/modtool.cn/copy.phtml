!if $error!=$empty
 !goto end
!endif

!if $originheader!=$empty and $job=move
 <b>警告</b>! 给模块改名会使引用它的作业崩溃.
除非你有绝对把握, 否则宁可
 !href cmd=reply&jobreq=copy 生成复本
.
 <p>
!endif

!if $target=$empty
 !form reply
 <input type=hidden name=jobreq value="$job">
 !distribute item 复制,改名,复制,改名 into P_copy,P_move,L_copy,L_move
 !if $originheader!=$empty
  $(L_$job)模块<em>$otit</em> (<tt>$original2</tt>)为<p>
 !else
  $(P_$job)公开模块<em>$otit</em> (<tt>$original2</tt>)到开发空间,
其地址为<p>
 !endif
 !read scripts/addressform.phtml
 </form>
 !if $originheader!=$empty and $o1 isitemof $zonelist,local
  <p>
  <b>说明</b>. 对公开地址的模块改名或复制有可能导致重复发布.
请千万小心!
  <p>
如果你要测试模块, 请将它放到测试区域.
 !endif
 !if $originheader!=$empty and ~ notin $original2
  <p>
  <b>说明</b>. 如果你已经发布了此模块,
请把它的地址改成公布地址. 否则会造成重复发布.
 !endif
!endif




:end
!if $original2!=$empty and $originheader=$empty
 !default otit=!module title $original2
 <p>
 !href module=$original2 $name_backto
 <p>
!endif


