
<center><h1>$tit</h1></center>

!if $error!=$empty
 !read msg.phtml
 <p><hr> <p>
!endif

!if $titb=$empty
 <p>
 El bloque <tt>$block</tt> de este documento no se creado todavía.
 <p>
 !exit
!endif

!if $datetest<0
 <p>
 Aún no se ha abierto esta página a la lectura. ¡Vuelva más adelante!
 <p>
 !exit
!endif

!if $datetest>0
 <p>
 Esta página ha expirado, ya no es posible leerla. ¡Lo sentimos!
 <p>
 !exit
!endif

<h3>$titb</h3>

!if $sheetdata=$empty
 <p>
 No hay datos de ejercicios en esta página.
 <p>
 !exit
!endif

Estos son los ejercicios de la página en formato de hoja de trabajo.
Puede cortar y pegar para insertarlos
!if $wims_user=supervisor
 en una hoja de su elección en la clase.
!else
 en una hoja de una clase virtual que le pertenezca.
!endif

<pre>
$sheetdata
</pre>

</div><div class=wimstail>
<p><hr><p>

<center>
!href cmd=reply&job=read Volver a doc
</center> <p>

