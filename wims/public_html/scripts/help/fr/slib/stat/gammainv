slib_lang_exists_tmp=yes

slib_title=Quantile de la loi Gamma

slib_out=Valeurs en <tt>data</tt> des quantiles de la loi Gamma\
 de paramètres <tt>a</tt> et <tt>b</tt> (2 réels  strictement positifs).
slib_comment=<tt>data</tt> peut être une liste de nombres entre 0 et 1.\
La densité en x est b*exp(-b*x)*(b*x)^(a-1)/gamma(a) pour x>=0

