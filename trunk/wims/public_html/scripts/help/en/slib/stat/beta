slib_lang_exists_tmp=yes

slib_title=Generation of beta random data

slib_out=Random data of <span class="tt">m</span> reals following a Beta law with parameters <span class="tt">a</span> and <span class="tt">b</span> (positive reals).\
If <span class="tt">m</span> is a list <span class="tt">[r,s]</span>, it returns an <span class="tt">r</span> by <span class="tt">s</span> matrix.\
The density at x is proportional to x^(a-1)(1-x)^(b-1) on the interval (0,1).

