!! +var+ +exec+ +string+
tit:product
type:$type_both
alias:!prod, !multiply
syntax:!product `fn' for `v'=`v1' to `v2' [step `st']\
$ !product `fn' for `v' in `v1,v2,v3,...'
mean:计算变量 `v' 逐个被 `v1' 到 `v2' 的值替换后函数 `fn(v)' 的乘积. <p>\
  在第二句里, 替换的值是表 `v1,v2,...' 里的各项.
