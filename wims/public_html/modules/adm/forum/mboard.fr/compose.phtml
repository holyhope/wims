
!if +$error iswordof +no_send_right
 !exit
!endif

!if $c_prec!=$empty
 Nouvelle suite à
 !href cmd=reply&job=read <b>$m_subject</b>
 par 
 !if $m_email!=$empty
  <i><a href="mailto:$m_email">$m_from</a></i>
 !else
  <i>$m_from</i>
 !endif
 <p>
!endif

!form reply
 Votre nom&nbsp;:
 !if $forumuser=$empty
  <input size=15 name=c_sender value="$c_sender">
 !else
  <tt>$c_sender</tt>&nbsp;;
 !endif
 email&nbsp;: <input size=30 name=c_smail value="$c_smail"> <br>
 !if $send_right<2
  <b>Attention.</b> Votre message ne pourra pas passer sauf si vous donnez
  une adresse électronique QUI MARCHE. <br>
 !endif
 Sujet&nbsp;:
 <input size=55 name=c_subject value="$c_subject"> <br>
 Contenu du message&nbsp;:<br>
 !set msgsrc1=<textarea cols=70 rows=18 name=wims_deposit wrap>
 !set msgsrc2=</textarea>
 !if $c_quote>0 and $c_prec!=$empty
  !mexec scripts/quote.sh
 !endif
 !if $c_quote=0
  !mexec scripts/msgsrc.sh
 !endif
 !if $c_quote<0
  $msgsrc1$msgsrc2
 !endif
 !set c_quote=0
 $tag_escaper</table>
 <p><center>
 <input type=submit name=c_preview value="Aperçu">
 &nbsp;&nbsp;
 <input type=submit name=c_send value="Envoyer">
 &nbsp;&nbsp;
 <input type=submit name=c_list value="Abandonner">
 </center> <p>
</form> <p>
!href cmd=help Aide
 sur comment écrire des formules mathématiques dans vos messages.

!if $c_prec!=$empty
 <p><hr> <p>
 !href cmd=reply&job=read Message d'origine
  par <i>$m_from</i>:<p>
 !read $forumdir/$c_prec html
 $tag_escaper</table></form><font size=+0>
 <p><hr> <p>
!endif

