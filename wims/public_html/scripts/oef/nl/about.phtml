!set email=!trim $email
!set author=!trim $author

!if $author!=$empty
Schrijver van deze oefening:
 !if $email!=$empty
  !mailurl $email $author
.
 !else
  $author.
 !endif
!endif
!if $solution!=$empty and $hint!=$empty
 Deze oefening heeft een hint en een uitgewerkte goede oplossing.
!endif
!if $solution!=$empty and $hint=$empty
 Deze oefening heeft geen hint , maar wel een uitgewerkte goede oplossing.
!endif
!if $solution=$empty and $hint!=$empty
 Deze oefening heeft een hint,  maar geen uitgewerkte oefening
!endif
!if $solution$hint=$empty
 Deze oefening heeft geen hint en ook geen uitgewerkte goede oplossing
!endif
Link naar deze oefening: <br>
!set exolink=$wims_ref_name?module=$module&cmd=new&exo=$thisexo
<a href=$exolink>$exolink</a> <br>
Link naar de huidige configuratie: <br>
!if $module_init_parm!=$empty
 !set exolink=$wims_ref_name?module=$module&cmd=new&$module_init_parm
!else
 !set exolink=$wims_ref_name?module=$module&cmd=new
!endif
<a href=$exolink>$exolink</a>
<p>
Information over deze verzameling oefeningen. <p>
$module_about
<p>
Het copyright van oefeningen berust bij de individuele auteurs.

!changeto NEWS.phtml
