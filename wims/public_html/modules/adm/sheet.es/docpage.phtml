!readproc adm/docsheet.prep
!read $docdef content
<p><center>
El registro de las puntuaciones está
 !set dd=!getscorestatus sheet=$sh
 !if $dd=yes
  !if $wims_scorereg!=suspend
   abierto, pero puede
   !href cmd=resume&job=suspend suspenderlo
.
  !else
   suspendido por usted mismo.
   !href cmd=resume&job=reopen Reactivarlo
.
  !endif
 !else
  cerrado para su conexión.
 !endif

!if $wims_user=supervisor
 <p>
 !href module=adm/class/sheet&sheet=$sh Página de gestión de las hojas de trabajo
.&nbsp;
 !set d_=!translate / to , in $sheet_docpage
 !distribute items $d_ into d_,b_
 !href target=wims_doc module=adm/doc.en&job=edit&doc=$d_&block=$b_ Editar esta página
.&nbsp;
 !href cmd=resume Recargarla
.
!endif
</center>

!tail

