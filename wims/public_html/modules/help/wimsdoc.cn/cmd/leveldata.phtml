!! +var+ +exec+ +string+
tit:leveldata
type:$type_both
alias:levelpoints
syntax:!leveldata size `xsize,ysize' ranges `x1,x2,y1,y2' function `f(x,y)' [levels `l1,l2,...']
mean:计算 `f(x,y)' 在水平 `l1,l2,...'\
  (默认值是一个水平 0) 处的水平曲线上的点的坐标. <p>\
  计算范围从 `(x1,y1)' 至 `(x2,y2)',\
  计算图形内关于 `xsize,ysize' 的整数位置的点. <p>\
  参数区段的次序不重要, 但除了水平外都是必须的.
