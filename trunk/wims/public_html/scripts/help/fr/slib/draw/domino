slib_lang_exists_tmp=yes

slib_title=Domino
slib_parms=6\
,nombre de points dans le premier carré\
,nombre de points dans le second carré\
black,couleur\
H,direction (V ou H)\
,texte si l'un des côté vaut 0\
raw,mot d'option : <span class="tt">url</span>, <span class="tt">html</span> or <span class="tt">raw</span>

slib_out=Dessin d'un domino (+ taille de l'image quand le mot d'option est \
<span class="tt">url</span> ou <span class="tt">html</span>).
slib_comment=Le premier item de la sortie est : par défaut, le code flydraw (mot d'option <span class="tt">raw</span>); \
l'adresse de l'image produite si le mot d'option est <span class="tt">url</span> ; \
le code html pour insérer l'image si le mot d'option est <span class="tt">html</span>.
