!set wims_module_log=error: $error

!if not_supervisor=$error
 Désolé, mais seul l'enseignant enregistré d'une classe a le droit d'écrire
 un message de la classe.
 !exit
!endif

!if bad_class=$error
 Etrange, mais je trouve que votre classe n'est pas valide!
 !exit
!endif

