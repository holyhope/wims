
$title (<tt>$mod</tt>)
<p>
!if $diff=$empty
 Nessuna differenza tra la versione in lavorazione e la versione pubblicata 
 su questo server.
!else
 !if $diff=MANY
  Non è possibile elencare tutte le differenze tra la versione in lavorazione
  e la versione pubblicata su questo server.
 !else
  Differenze tra la versione in lavorazione e la versione pubblicata su
  questo server:
  <pre>$diff</pre>
 !endif
!endif
<p><hr> <p>

