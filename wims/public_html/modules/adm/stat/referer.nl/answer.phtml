!set week_min=3
!set month_min=20
!set year_min=200

!set n=!linecnt $ref
!set name_site=the site
!set name_stem=a page under the address
!set name_uniq=the page
!set names_site=sites
!set names_stem=site directories
!set names_uniq=pages
List of web $(names_$type) bringing visitors to us, within the period of 
$month1-$day1-$year1 to $month2-$day2-$year2 (sum over $laps days).
<br>The first column gives the number of our visitors who followed
a link in $(name_$type) on the second column.
<p>
<table border=0 cellspacing=1>
!set total=0
!set totall=0
!for i=1 to $n
 !set l=!line $i of $ref
 !set w=!wordcnt $l
 !set a=!word 1 of $l
 !set b=!word 2 of $l
 !if $w=2 and ($search = yes or (search notin $b and query notin $b and cgi notin $b))
  !set totall=$[$totall+1]
  <tr><td align=right>$a<td>&nbsp;&nbsp;
  <td><a href="http://$b">$b</a> 
  !set total=$[$total+$a]
 !endif
!next i
<tr><td align=right>$total<td>&nbsp;&nbsp;
<td>people have visited us through links in these $totall $(names_$type).
</table> <p>

!if $auto>0
 At least $auto other visitors didn't specify public referring pages. Referring pages
 within $httpd_HTTP_HOST are not counted.
 <p>
!endif

