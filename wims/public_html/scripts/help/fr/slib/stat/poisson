slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire suivant la loi de Poisson.

slib_out=Simulation de <tt>m</tt> réalisations d'une variable aléatoire suivant la loi de Poisson\
de paramètre <tt>lambda</tt> (nombre positif).

slib_comment= Pour un entier positif,  P(X=i)=exp(-lambda)*(lambda^i)/i