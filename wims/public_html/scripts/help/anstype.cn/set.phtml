 <b>有限集合(按字面区别的、形式的或近似的).</b> (名称: <tt>set</tt>, <tt>fset</tt> 及 <tt>aset</tt>) <br>
 这是一列以逗号相隔的元素表, 其次序无关紧要. 
<p>
 
对于 <tt>set</tt>, 各元素只进行字符比较而无求值过程 (因此 2 和 1+1 不代表同一元素).
 <p>
对于  <tt>fset</tt>, 其中元素被看作<em>形式表达式</em>予以求值.
 <p>
对于 <tt>aset</tt>, 其中元素被看作<em>近似表达式</em>予以求值.

