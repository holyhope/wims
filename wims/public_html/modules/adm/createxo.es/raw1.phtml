!form reply
<input type=hidden name=level value=3>

!if $existing=$empty
 Está en el modo en bruto para la creación de ejercicios. Este modo
 está reservado para expertos o para registrar un ejercicio del
 cual tiene el código fuente. Para crear un ejercicio a partir de cero
 mejor utilice el
 !href cmd=reply&mode=normal modo guiado
.
 <p>
 Introduzca el código fuente completo (
 !href target=wims_mhelp cmd=help&special_parm=oef formato OEF
 ) de su ejercicio:
 (para registrar un fichero fuente ya existente, copie el contenido el archivo en 
 la ventana de texto).
!else
 Este es el código fuente de un ejercicio 
 !if $catmodule=$empty
  existente en su clase. Puede modificarlo
  en la ventana inferior.
 !else
  público que quiere importar.
  Puede modificarlo en la ventana inferior, antes de añadirlo a la clase.
 !endif
!endif

<p><center>
!mexec textarea.sh
<p><input type=submit value="Enviar el código fuente"></center>
</form>

