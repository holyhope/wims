slib_lang_exists_tmp=yes

slib_title=Simulation de réalisations d'une variable aléatoire suivant la loi de Weibull.

slib_out=Simulation de <span class="tt">m</span> réalisations d'une variable aléatoire suivant la loi de Weilbull\
de paramètres  <span class="tt">a</span> et <span class="tt">b</span> (réels strictement positifs).\
Si <span class="tt">m</span> est un vecteur à deux éléments entre crochets <span class="tt">[r,s]</span>, \
renvoie une matrice à <span class="tt">r</span> lignes et <span class="tt">s</span> colonnes.
slib_comment= La densité est a*b*exp(-b*x^a)*x^(a-1) pour x&gt;0.



