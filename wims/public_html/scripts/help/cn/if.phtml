所有的比较都在串上进行: <tt>string1 rel string2</tt>.
可以利用 <tt>and</tt> 与 <tt>or</tt> 把比较结合起来. 
还可以利用括号以构成复杂的比较式.

:有效的关系 <tt>rel</tt>:
:关系,条件
:= </tt> 或 <tt>==
true(真), 若 <tt> string1</tt> 与 <tt>string2</tt> 完全相同.
:!= </tt> 或 <tt>&lt;&gt;
true(真), 若 <tt> string1</tt> 与 <tt>string2</tt> 不相同.
:<
true(真), 如果两个串的数值 <tt>string1</tt> < <tt>string2</tt>.
:<=
true(真), 如果两个串的数值 <tt>string1</tt> $m_le <tt>string2</tt>.
:>
true(真), 如果两个串的数值 <tt>string1</tt> > <tt>string2</tt>.
:>=
true(真), 如果两个串的数值 <tt>string1</tt> $m_ge <tt>string2</tt>.
:isin
true(真), 若 <tt>string1</tt> 是 <tt>string2</tt> 的子串.
:notin
true(真), 若 <tt>string1</tt> 不是 <tt>string2</tt> 的子串.
:iswordof
true(真), 若 <tt>string1</tt> 是 <tt>string2</tt> 的一个词.
:notwordof
true(真), 若 <tt>string1</tt> 不是 <tt>string2</tt> 的一个词.
:isvarof
true(真), 若 <tt>string1</tt> 是表达式 <tt>string2</tt> 的一个(数学)变量.
:notvarof
true(真), 若 <tt>string1</tt> 不是表达式 <tt>string2</tt> 的一个(数学)变量.
:isvariableof
true(真), 若 <tt> string1</tt> 是表达式 <tt>string2</tt> 的一个(数学)变量.
:notvariableof
true(真), 若 <tt> string1</tt> 不是表达式 <tt>string2</tt> 的一个(数学)变量.
:isitemof
true(真), 若 <tt> string1</tt> 是表 <tt>string2</tt> 的一项.
:notitemof
true(真), 若 <tt> string1</tt> 不是表 <tt>string2</tt> 的一项.
:islineof
true(真), 若 <tt> string1</tt> 是表 <tt>string2</tt> 的一行.
:notlineof
true(真), 若 <tt> string1</tt> 不是表 <tt>string2</tt> 的一行.
:issamecase
true(真), 若 <tt> string1</tt> 与 <tt>string2</tt> 有相同的文字, 比较时不考虑多余的空格, 但是区别大小写字母.
:notsamecase
true(真), 若 <tt> string1</tt> 与 <tt>string2</tt> 有不相同的文字, 比较时不考虑多余的空格, 但是区别大小写字母.
:issametext
true(真), 若 <tt> string1</tt> 与 <tt>string2</tt> 有相同的文字, 比较时不考虑多余的空格、大小写字母及有重音号的字母.
:notsametext
true(真), 若 <tt> string1</tt> 与 <tt>string2</tt> 有不相同的文字, 比较时不考虑多余的空格、大小写字母及有重音号的字母.

