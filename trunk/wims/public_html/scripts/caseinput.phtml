
!if $module_language=fr
<p>
Si votre clavier n'a pas de lettres avec accent : vous pouvez taper
 <span class="tt">e'</span> pour é, <span class="tt">a`</span> pour à, <span class="tt">i"</span> pour ï, 
 <span class="tt">o^</span> pour ô, <span class="tt">n~</span> pour ñ, <span class="tt">c~</span> pour ç, etc.
 </p>
 !exit
!endif
!if $module_language=nl
<p>
 Om letters met accenten in te typen op een keyboard zonder deze, kunt U 
 <span class="tt">e'</span> voor é, <span class="tt">a`</span> voor à, <span class="tt">i"</span> voor ï, 
 <span class="tt">o^</span> voor ô, <span class="tt">n~</span> voor ñ, <span class="tt">c~</span> voor ç, enz. gebruiken
 </p>
 !exit
!endif
!if $module_language=es
<p>
 Si su teclado no tiene las teclas con los acentos puede teclear
 <span class="tt">e'</span> para escribir é, <span class="tt">a`</span> para à, <span class="tt">i"</span> para ï, 
 <span class="tt">o^</span> para ô, <span class="tt">n~</span> para ñ, <span class="tt">c~</span> para ç, etc.
 </p>
 !exit
!endif

<p>
To type accented letters on a keyboard without them: you may enter
<span class="tt">e'</span> for é, <span class="tt">a`</span> for à, <span class="tt">i"</span> for ï, 
<span class="tt">o^</span> for ô, <span class="tt">n~</span> for ñ, <span class="tt">c~</span> for ç, etc.
</p>

