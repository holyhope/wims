!! +var+ +exec+ +string+
tit:solve
type:$type_both
alias:rootof
syntax:!solve `fn' for `v'=`v1' to `v2'
mean:找出变量 `v' 在范围 `v1' 与 `v2' 内, 函数或方程 `fn' 的(实单)根. <p>
  `fn' 在区间 [v1,v2] 内可能有几个根, 但太接近的根可能被忽视.</p>
