!set wims_module_log=error: $error

!if not_supervisor=$error
对不起, 只有班级的注册主持教师能进入此页.
 !exit
!endif

!if bad_class=$error
奇怪, 你的班级是无效的!
 !exit
!endif

!if too_many_users=$error
你的班级有太多参加者($ucnt).
只能对至多 $maxuser 参加者的班级手工输入评分. 抱歉.
 !exit
!endif

!msg $error

