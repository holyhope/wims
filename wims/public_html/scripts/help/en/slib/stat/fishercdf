slib_lang_exists_tmp=yes

slib_title=Fisher cumulative distribution function. 

slib_out=Values on <span class="tt">data</span> of cumulative distribution function of the Fisher law with parameters <span class="tt">a</span> and <span class="tt">b</span> (two positive integers).

slib_comment=<span class="tt">data</span> can be a list of reals in brackets.

