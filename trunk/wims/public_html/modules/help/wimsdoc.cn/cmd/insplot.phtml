!! +exec+ +ins+
tit:insplot
type:$type_pexec
syntax:!insplot `plotsource'
mean:服务器调用外部绘图程序(当前版本使用gnuplot), 把经过变量替换的\
  `plotsource' 作为 2D 描图命令送过去, 把结果插入页面. <br/>\
  `plotsource' 的语法就是 gnuplot 命令 `plot' 的语法.
