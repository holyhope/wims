<center><h1>$tit</h1></center>

!if $error!=$empty
 !read msg.phtml
 <p><hr> <p>
!endif

!if $erase!=$empty
 De file <b>$erase</b> is gewist
 <p>
!endif

!if $quota_free<1
U kunt geen files meer invoeren in dit document, omdat de inhoud van dit
document reeds aan de grens zit van de maximale filegrootte op deze server.

!else
 !set wims_form_method=file
 !form reply
 <input type=hidden name=job2 value=deposit>
 U kunt een bestand invoeren in dit document:
 <input type=file size=35 name=wims_deposit>
 <input type=submit value=OK>
 </form>
!endif

!if $flist!=$empty
 <p>Beschikbare files in dit document:
 !for f in $flist
  !getfile doc/$f $f
  &nbsp;
 !next f
 <p>
 !form reply
 <input type=hidden name=job2 value=erase>
 Verwijder een bestand:
 !formselect job2 list $flist
 <input type=submit value=OK>
 </form>
 <p>
 De bestanden in dit document kunnen worden aangeroepen met de variabele <tt>\filedir</tt>
 Bijvoorbeeld, 
<p><center>
 <tt>&lt;a&nbsp;href=\filedir/$f1&gt;$f1&lt;/a&gt;</tt>
</center> <p>
 geeft een link naar de file <a href=$m_filedir/$f1>$f1</a>.
 U kunt ook schrijven;
<p><center>
 <tt>&lt;img src=\filedir/myfile.jpg&gt;</tt>
</center> <p>
om het plaatje <tt>myfile.jpg</tt> in deze pagina in te voegen.
 <p>
 De gedeponeerde bestanden in dit document zijn alleen bereikbaar voor mensen
 met leesrechten.
!endif

<p>
Totaal beschikbare hardeschijf ruimte: $quota_total Meg. 
Vrije harde schijfruimte: $quota_free Meg.

<p><center>
!href cmd=reply&job=prop Document eigenschappen
&nbsp;&nbsp;
!href cmd=reply&job=list Blok lijst
&nbsp;&nbsp;
!href cmd=reply&job=read&block=main Hoofd blok

</center> <p>

