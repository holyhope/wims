!! +exec+ +html+
$lst_tit restart$lst_titend$type_exec
  $(lst_syntax)restart $emph parm$emphend</tt>
  $lst_mean 重新开始带有参数 $emph parm$emphend 的新请求, 通常是跳到另一模块.
  <p>
  此命令只能用在可信任模块里, 而且只能用在开始输出之前.
  不能重复重启, 以避免无穷循环.


