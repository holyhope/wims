
!if $levelcnt<=0
 !exit
!endif
Hier is de backupfile:

!getfile gateway.csv gateway.csv
<p>
Dit is een komma-gescheiden datafile. De betekenis van de velden:
<p>
type, code,
titel, supervisor-account, klasse-wachtwoord, supervisor-wachtwoord, opties.

