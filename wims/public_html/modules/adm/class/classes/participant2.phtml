
   <td align="center" colspan="2">xxx
    !if $class_auth iswordof cas
     !set url = $wims_ref_name?module=adm/class/classes&type=authparticipant&class=$code
     !read adm/codeurl $url
     <a class="wims_button" href="$class_cas_host/login?service=$script_out">$wims_name_Enter</a>
    !endif
    !if $class_auth iswordof idpphp
      <a class="wims_button" href="$class_phpidp_url?enter=$code">$wims_name_Enter</a>
   !endif
   !if $class_auth iswordof ldap
     !set wims_ref_class=wims_button
     !href module=$module&type=authparticipant&class=$code $wims_name_Enter
   !endif
   </td>